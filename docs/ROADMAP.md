# 项目路线图

路线图用于管理实施顺序；详细产品规则和验收标准以核心项目文档为准。状态变化统一同步到 `STATUS.md`。

## M0：项目基线

状态：已完成

- [x] 核心需求与技术方案 v1.1
- [x] Eagle 正式目录和基础标签组
- [x] 项目优先的待入库结构
- [x] Git 仓库与忽略规则
- [x] 长期项目管理文档

## M1：文字检索闭环

状态：已完成

- [x] SQLite 项目、包装、版本、文件映射和查询状态表
- [x] 数据库迁移与自动化测试数据
- [x] Eagle 素材读取和映射完整性校验
- [x] 项目名、项目别名、包装类型和标签检索
- [x] 最多三个 PNG 候选回复
- [x] 原查询人文字确认与 20 分钟过期控制
- [x] ZIP 回复到最初查询消息
- [x] 重复确认幂等控制
- [x] 第一阶段验收测试（测试群端到端实测：查询、候选、确认、直发与云盘链接）

## M2：版本与模糊输入

状态：进行中

- [x] 已入库包装管理：预览/ZIP 修订、新包装新增入口
- [x] `package_revisions` 修订记录和 `base_package_id` 新版本草稿
- [ ] 通用包装、多项目复用和项目修改版的查询排序
- [ ] 错别字、别名和近义词匹配
- [ ] 权限、入库校验和异常恢复

## M3：图片识别辅助

状态：未开始

- [ ] 下载飞书参考图片
- [ ] 包装区域和类型辅助识别
- [ ] 指定项目范围内的视觉相似度排序
- [ ] 无项目名时的多候选召回
- [ ] 外部识别服务隐私开关和明确授权

## M4：运行保障

状态：进行中

- [ ] X.bot 与 Eagle 启动依赖检查
- [ ] `FeishuBot` 登录自动启动：启动脚本和单实例保护已实现，但当前计划任务未注册
- [x] 事件消费者非零退出的退避重试与受控恢复
- [ ] 健康检查和断线重连
- [ ] 失败重试、结构化日志和故障诊断
- [ ] 基于真实使用记录优化排序

## M5：AE 包装提取与 Eagle 入库工具链

状态：进行中（真实 AE 主程序验收、重复导入三选项、SQLite 联调、Report 解析和 CI 已完成；中央索引服务与真实现场验证仍待收尾）

- [x] 外部工具不启动 AE 即显示工程内全部合成、父级使用关系和内部预合成，并支持多选目标合成
- [x] 从指定预览帧生成静态 PNG，并记录透明通道要求
- [x] 按选中合成递归收集嵌套合成和图层素材，在新目录生成精简 AEP、素材目录和 manifest，并自动重新解析自检
- [x] 每个合成收集完成后自动生成包含精简 AEP、素材和 manifest 的独立 ZIP，并执行 CRC 与必需文件校验
- [x] 区分 `xbot-collection` 收集记录与 `xbot-eagle-ingest` 正式入库清单，收集记录可与根目录 PNG 安全配对且不会触发全局忽略
- [x] 将逐合成 ZIP 与代表帧 PNG 形成完整交付单元，默认第 2 秒且可在预览窗口调帧
- [x] 支持查看选中合成的直属预合成，并将各直属预合成分别生成独立 AEP、PNG、manifest 和 ZIP
- [x] 视频框使用父级展示合成预览时，按嵌入图层的 `in_point + 2 秒` 取帧并限制在可见区间内
- [x] 读取收集报告中的合成、字体、效果和素材依赖；支持中英文章节、UTF-8/UTF-16/GB18030 编码，并将缺失素材阻止性与字体/效果警告接入插件预检
- [x] 生成包含项目、包装、版本、合成名和依赖状态的 manifest
- [x] 对跨合成表达式、缺失素材、字体和第三方效果显示人工复核警告
- [x] 旧 ScriptUI 方案已验证 AE 原生 Collect Files 和 Windows ZIP 能力；按 D-026 保留为兼容/预览能力，不再作为离线收集主入口
- [x] 在交付 Eagle 前检查 PNG、ZIP、manifest 与阻止入库风险
- [x] 用真实“阿宝包装.aep”验证收集、压缩、代表帧、真实 AE 打开验收和 Eagle 配对；Report 正文解析已完成，新产物正式归位仍需真实现场联调
- [x] Eagle 窗口插件框架
- [x] 支持拖入或选择一个本地项目包装文件夹，并在 `00_待入库` 下创建对应批次文件夹
- [x] 递归扫描源目录，优先读取 manifest，只导入规则允许的 PNG 和 ZIP
- [x] 将扫描结果分为“将导入、仅校验、忽略、冲突”四种状态
- [x] 为批次生成 `batch_id`
- [x] PNG、ZIP 与 manifest 自动配对
- [x] 缺失信息和冲突检查界面
- [x] 检测重复文件夹导入并提供复用、更新或新建选择
- [x] 人工确认、正式分类和标签写入（代码与自动化测试完成，待真实 Eagle 界面复验）
- [x] 正式入库独立递归扫描 Eagle 待入库批次，支持无插件注释的手动导入文件夹和缺失类型补充
- [x] SQLite 稳定映射同步（插件 JSONL 事件 → bot 单写入 SQLite → 强制刷新 Eagle 索引，支持幂等和失败重试）
- [x] 与 X.bot 服务边界和兼容接口（共享运行根目录事件队列，bot 监听器负责消费）
- [x] GitHub Actions 在 Windows、Node.js 24 和 Python 3.14 环境自动运行测试与静态检查

## 未来候选项

以下内容必须经过新的范围确认后才能进入正式里程碑：

- 自动识别工程中可能具有复用价值的合成
- 包装版本之间的 AE 工程差异展示
- 敏感项目的用户/群聊白名单
- 使用历史驱动的个性化排序
- 入库批量审核和版本差异展示
