# 项目当前状态

最后更新：2026-09-03

## 状态摘要

| 项目 | 当前值 |
| --- | --- |
| 项目阶段 | M1 文字检索闭环已完成；M5 已完成外部收集、逐合成 ZIP、可调代表帧 PNG、真实 AE 主程序验收、Eagle 安全配对、插件→SQLite→bot 索引联调和 CI，剩余 Report 解析与真实现场验证 |
| 核心文档版本 | v1.26 |
| 软件版本 | `0.0.0` 开发版，尚未发布标签 |
| 当前分支 | `codex/aep-precomp-ci` |
| 远程仓库 | GitHub：`OooT-x/xbot-packaging-search`（私有），`codex/feishu-bot-m1` 与 `codex/eagle-ingest-plugin` 均已推送 |
| Eagle 库 | `E:\Eagle资源库\包装.library` |
| 当前包装索引 | 4 个项目、19 组包装：乾崑奕境 5 组、支付宝阿宝 6 组、华为途灵 5 组、变速箱 3 组，配对错误 0 |
| 应用源码 | 已迁入既有 X.bot 监听器，并接入包装检索模块 |
| 最近交付 | AEP 直属预合成独立导出、视频框父级取帧修复，以及 GitHub Actions 持续集成 |
| 测试策略 | 后续测试以精简、精准为约束，只保留新增行为、关键边界和回归风险的最小有效覆盖 |

## 已完成

- [x] 新增 GitHub Actions 持续集成：push、pull request 和手动触发时，在 `windows-latest` 上使用 Node.js 24 与 Python 3.14 安装收集器依赖，并自动执行 `npm test` 和 `npm run check`；当前本地验证为 96 项 JS + 23 项 Python 测试通过。
- [x] 飞书消息类型兼容：compact `post` 中的文字、`@` 和图片占位会规范化后进入现有 AI 对话、包装查询和候选确认链路；纯图片在私聊、有效 `@X.bot` 或群聊回复 X.bot 时回应，但不下载图片、不调用外部视觉模型、不伪装识图。新增富文本规范化、图片门控和富文本确认回归测试；85 项 JS + 18 项 Python 测试全部通过。
- [x] 整理项目本地产物：删除 `aep-collector/` 下可重建的 `.build-venv`、`build`、`__pycache__` 及 `dist/AE-Preview-Bridge` 源码冗余副本，保留交付物 `dist/XbotAepCollector.exe` 与活跃的运行数据/日志；`aep-collector` 由约 130MB 精简至约 28MB，git 工作区干净（变更见 `CHANGELOG.md`）。
- [x] AI 对话能力增强 + 活人感提升：系统提示精简重写（20+ 规则→12 核心原则）、回复风格变化追踪（`analyzeRecentStyle` 检测重复模式并注入变化提醒）、情绪感知扩展（烦躁/疲惫模式）、消息意图分类器、包装查询主动建议。81 项 JS + 18 项 Python 测试全部通过。

- [x] 新增 `docs/CONTEXT.md` 轻量上下文速览（状态摘要、里程碑、模块地图、关键决策指针、数据/安全边界、常用命令），并将它接入 `AGENTS.md` 必读顺序与 `README.md` 项目管理入口：后续迭代先读速览，命中对应主题时才翻开长文档，降低每次恢复上下文的阅读量。
- [x] 将暂不实施的 Codex 路线升级方案归档到 `docs/deferred/`；不纳入 `docs/CONTEXT.md`，仅在明确讨论 Codex 路线时读取。
- [x] 修复 bot 登录自启动并增强单实例保护：注册 `FeishuBot` 登录自启计划任务（指向 `scripts/start-feishu-bot-autostart.ps1`）；`Get-ExistingBotProcess` 在 pid 文件校验之外，增加按 `lark-bot-listener.js` 运行进程扫描兜底，避免因运行时根不一致导致重复启动 bot。
- [x] 2026-08-24 恢复运行态：重新注册 `FeishuBot` 登录自启任务，动作显式传入既有飞书运行根目录；监听器在 `event consume` 非零退出时记录错误并按 5 秒起步、最多 60 秒退避重试。受控终止事件消费者后，父进程自动恢复监听，`active_consumers=1`。
- [x] 2026-08-24 完成群历史读取权限：为 X.bot 发布 `im:message.group_msg` 后，bot 身份的 `+chat-messages-list` 对目标群读取成功；已重启常驻任务，事件总线保持 `running` 且 `active_consumers=1`。
- [x] 单候选和多候选统一为一条飞书富文本消息；每个候选名称紧跟对应图片，选择提示和有效期不再拆成第二条消息。
- [x] 移除包装确认和类型追问的无引用历史回退；只有回复机器人候选或提示消息时才读取查询语境，避免旧 `pending` 查询污染新消息。
- [x] 确认核心需求、数据模型、消息流程和验收标准。
- [x] 建立 Eagle 正式目录：`01_预览图`、`02_AE源文件`及四类子目录。
- [x] 建立 Eagle 基础标签组：包装类型、信息用途、画面结构、背景用途。
- [x] 新增 Eagle“项目”标签组，并将“支付宝阿宝”“华为途灵”“乾崑奕境”“变速箱”4 个项目名标签归入该组；保留原有标签和素材关联。
- [x] 建立项目优先的 `00_待入库` 目录。
- [x] 将 Eagle 入库助手插件安排到第五阶段。
- [x] 将 AE 工程中的包装提取、依赖整理和待入库清单生成纳入第五阶段工具链。
- [x] 明确本地项目包装文件夹按一个 Eagle 待入库批次导入，并采用规则扫描而非原样递归导入。
- [x] 通过 Eagle Web API V2 完成首个真实批次“变速箱包装”的人工审核入库：3 组 PNG + ZIP 统一登记为 `v01`，写入项目、类型、批次、配对关系及依赖警告，并移入正式分类目录。
- [x] 将首批真实入库经验沉淀为正式规则：Eagle 入库不复核已打包 ZIP 内依赖的必要性，素材描述只保留稳定元数据和依赖事实，不写审核过程说明。
- [x] 按 v1.4 规则重新执行“变速箱包装”批次入库：当前正式目录包含 3 组 PNG + ZIP，待入库素材为 0，描述和配对校验全部通过。
- [x] 在错误递归导入项目退出活动库后，按 v1.5 规则从干净批次 `batch-MSZQTI9M9MJOZ` 重新入库“变速箱包装”：活动素材仅保留 3 组 PNG + ZIP，正式分类各 1 项，待入库整棵素材数为 0，配对、标签、描述和依赖警告复核通过。
- [x] 明确正式入库完成后可以清理整棵为空的批次文件夹；删除前必须递归确认素材数和子目录内容均为空。
- [x] 建立 SQLite 项目、包装、文件映射、查询候选和发送幂等数据结构及迁移工具。
- [x] 通过 Eagle Web API V2 同步首批真实素材；首批索引为 1 个项目、3 组 PNG + ZIP，配对错误为 0。
- [x] 2026-08-21 运行索引已扩充为 2 个项目、9 组包装：变速箱 3 组、支付宝阿宝 6 组，配对错误为 0。
- [x] 实现项目名、项目别名、包装类型和标签驱动的最多 3 个候选排序。
- [x] 将包装查询接入既有 X.bot Node 监听器，保留群聊显式 `@mention` 门控和原有回复能力。
- [x] 实现 PNG 候选、原查询人回复确认、20 分钟过期、ZIP 回复原查询消息和重复确认幂等控制。
- [x] 完成 19 项自动化测试、Node 静态检查，以及真实 PNG/ZIP 飞书回复请求 dry-run。
- [x] 修复飞书接口幂等键超长（>50 字符）导致包装查询发送失败的问题，统一改用 48 字符确定性哈希。
- [x] 曾实现无回复引用时回退最近待确认查询；该行为已由 D-032 取代并移除。
- [x] 包装候选回复去掉“依赖警告”标签，并将提示文案调整为更自然的口语表达。
- [x] 放宽包装查询触发条件：项目名命中为主闸门，动作词或领域词命中其一即可触发，保留 `@X.bot` 门控。
- [x] 包装查询接入大模型语义判断兜底：规则未命中且 `@X.bot` 时由 AI 判断意图并提取项目/类型，失败自动回退规则。
- [x] 预览/提示消息下的回复支持语境延续：如“看看背景”会继承当前项目并切换到新类型候选。
- [x] 多候选预览合并为一条飞书富文本消息，数字选择确认，“这个”在多候选时提示明确选择。
- [x] AI 提供方默认统一为 DeepSeek，启动脚本与代码不再自动回退 Ollama/OpenAI。
- [x] 候选回复文案优化：候选行只显示项目、包装名和版本，不显示包装类型。
- [x] 源文件发送幂等改为按单个候选记录：多候选查询内每个选项可分别发送，重复确认同一候选才拦截。
- [x] 已完成的历史查询不再接受任何候选发送，确认流程补充错误日志便于排查静默失败。
- [x] 包装索引同步默认改为只读解析指定 Eagle 库目录，不依赖 Eagle 当前活动库，Eagle 界面可停留在其他库。
- [x] 超限源文件自动上传 bot 飞书云盘并回复下载链接，实测 61.7MB 视频框 ZIP 上传成功且链接可下载。
- [x] 语境延续不再取消前一条查询，多条候选查询并存，回复任一旧预览仍可确认。
- [x] 初始化 Git 仓库并纳入核心项目文档。
- [x] 建立长期项目管理规则、路线图、状态页、决策记录和变更日志。
- [x] 测试群端到端实测通过：真实查询返回候选、原查询人确认、小文件直发、超限视频框 ZIP 云盘链接下载。
- [x] 触发条件放宽并接入大模型语义判断兜底，保留 `@X.bot` 门控。
- [x] 多候选预览合并为一条富文本消息，数字选择确认；语境延续支持“看看背景”切换类型。
- [x] 多条候选查询并存，语境延续不取消旧查询，回复旧预览仍可确认。
- [x] 源文件发送候选级幂等：同一查询内不同候选可分别发送，重复确认同一候选才拦截。
- [x] 索引同步默认只读解析 `E:\Eagle资源库\包装.library`，不依赖 Eagle 当前活动库。
- [x] 超限源文件自动上传 bot 飞书云盘并回复下载链接。
- [x] AI 提供方默认统一为 DeepSeek。
- [x] 建立 Eagle 窗口插件骨架：`manifest.json`、logo、文件夹选择、扫描预检、四状态表格和确认导入按钮。
- [x] 实现插件扫描引擎：递归扫描、manifest 优先引用配对、无 manifest 时受控候选配对、`batch_id`/`batchKey`、将导入/仅校验/忽略/冲突四状态。
- [x] 实现 Eagle 导入层：`00_待入库/<项目>包装` 批次文件夹创建与复用、`addFromPath` 导入 PNG/ZIP、D-013 事实字段描述与标签、配对 Eagle ID 回写。
- [x] 插件拆分为“导入”与“入库”两个按钮：导入只落 `00_待入库/<项目>包装` 批次；入库按钮读取指定待入库批次，校验 PNG+ZIP 配对后按类型归位到 `01_预览图/<类型>` 与 `02_AE源文件/<类型>`，并将 `00_待入库` 标签替换为正式目录标签。
- [x] 修复入库批次读取：兼容“配对文件未挂到批次文件夹”的情况，按配对引用和 `batch_id` 自动召回未分类配对文件；刷新批次后自动加载入库预检。
- [x] 修复同一待入库文件夹混有多个 `batch_id` 时相同 `package_id` 被错误合并判重的问题：现在按 `batch_id + package_id` 配对；旧的不完整批次不会遮蔽新完整批次，两个完整重复批次仍阻止入库。
- [x] 批次刷新优先通过 Eagle `item.get({ folders })` 读取所选文件夹，再查询未分类素材补齐配对关系；界面显示识别到的 `batch_id` 数量及每条记录的批次 ID。
- [x] 插件自动化测试共 24 项（扫描 9 项、导入 5 项、正式归位与批次读取 10 项），全项目测试总数 59 项全部通过。
- [x] 正式入库不再依赖插件导入注释：递归读取 Eagle 待入库批次及子目录，支持用户直接使用 Eagle 文件夹导入的素材。
- [x] 对无注释 PNG/ZIP 按受控位置、文件名和目录名重新配对；非候选依赖文件忽略，歧义和缺配对继续阻止。
- [x] 唯一配对但缺少包装类型时进入“待补充”，用户可直接选择信息条、视频框、背景或分镜排版；正式归位时补写统一注释、标签、配对 ID 和规范名称。
- [x] 插件自动化测试增至 30 项，全项目测试总数 65 项全部通过。
- [x] 修复真实 Eagle `folder.getAll()` 返回树形目录而适配层按扁平列表处理的问题：递归展开全部子目录并补全父级关系；同时兼容素材 `folders` 字段为 ID 或文件夹对象。
- [x] 通过当前 Eagle Web API 对手动导入的 `变速箱包装` 做真实只读验证：递归发现批次根目录与 `Root_Collected_Projects`，识别 6 个无注释文件，得到背景、视频框 2 对可入库及小标注 1 对待补充类型；全项目测试增至 68 项。
- [x] 根据真实 `v1.1.1` 窗口日志修复 Folder 实例 getter 丢失：显式保留 `name`、`description` 和 `children`，避免真实存在的 `00_待入库` 被判断为不存在；全项目测试增至 69 项。
- [x] 统一包装类型别名推断：“标注”“人名条”在本地扫描、Eagle 手动批次扫描和 manifest 解析中直接规范化为“信息条”，仍允许预检时人工改正。
- [x] 真实 Eagle 手动导入批次正式归位成功：3 张 PNG 与 3 个 ZIP 均进入对应正式类型目录，标签、描述、稳定 ID 和配对 ID 完整；待入库批次当前为空。
- [x] 修复飞书 bot 新查询与候选确认的路由冲突；后续 D-032 进一步移除全部无回复引用回退。
- [x] 建立 `ae-tool/XbotPackagingOrganizer.jsx` 可停靠 ScriptUI 面板，读取 Project 面板中用户明确选择的一个或多个包装合成，并逐条维护包装名称、类型、版本、代表帧和透明通道要求。
- [x] 实现 AE 上游保守风险扫描：递归检查嵌套合成中的缺失素材、字体、效果、疑似第三方效果和常见跨合成表达式引用；缺失素材、外部合成引用和扫描失败标记为“阻止入库”，字体与疑似第三方效果标记为“警告”。
- [x] 实现代表帧 PNG、标准化 manifest、AE 原生 Collect Files 引导、Windows 收集目录 ZIP 压缩和交付检查；生成结果已通过 Eagle 扫描器自动化配对测试。
- [x] 将面板安装到本机 AE 2025 `ScriptUI Panels` 并完成哈希一致性验证；真实 AE `25.6.4x3` 运行时探针确认工程、ScriptUI 和 `saveFrameToPng` 接口可用，面板已从安装目录加载，并从当前所选“标注-方括号”合成的 3.8 秒成功输出 18,749 字节 PNG。
- [x] 新建 `aep-collector` Windows 外部工具，基于 MIT 许可的 `py-aep` 直接解析 AEP，不启动 After Effects，也不复制或重新分发 BHY Collect Pro 的闭源 EXE。
- [x] 外部工具显示工程内全部合成，并展示尺寸、时长、帧率、父级使用关系和内部预合成；真实“阿宝包装.aep”识别 42 个项目项、11 个合成，与离线解析日志一致。
- [x] 实现按选中合成安全收集：递归保留嵌套合成和图层素材，复制并重链接素材，在新目录输出精简 AEP 与 manifest，保存后自动再次解析自检，原工程不写入。
- [x] 修复 `py-aep 0.10.2` Reduce Project 后保留已删除活动合成 ID、导致输出 AEP 无法重解析的问题；收集器同步修复 `fcid` 和活动项目引用。
- [x] 真实收集验证通过：“背景”输出 1 个合成、9 个素材且全部重链接到新目录；“包装”输出 11 个嵌套合成、10 个素材且全部路径存在。新增 5 项 Python 单元测试，全项目自动化测试目标增至 84 项。
- [x] 生成 `XbotAepCollector.exe`，显式打包 Python 3.14 的 Tcl/Tk 运行时并完成 6 秒启动存活检查。
- [x] 每个选中合成在收集完成后自动生成一个同名 ZIP；ZIP 使用独立顶层目录封装精简 AEP、素材和 manifest，生成后检查 CRC 以及 AEP/manifest 必需项，失败不保留半成品。新增 2 项 Python 单元测试，全项目 86 项自动化测试全部通过。
- [x] 使用真实“阿宝包装.aep / 背景”验证逐合成打包：ZIP 为 21,270,338 字节、包含 11 个文件且 CRC 正常；包内 AEP 和 manifest 完整，展开工程仍只含目标合成，9 个素材链接全部存在，manifest `source_file` 与 ZIP 同名。
- [x] 建立双 manifest 协议：收集器写入 `xbot-collection`，AE 内正式交付清单写入 `xbot-eagle-ingest`；Eagle 插件只对正式入库清单启用“仅导入明确引用文件”，并兼容未带类型字段的旧版两类 manifest。
- [x] Eagle 插件 `1.1.4` 支持从收集记录读取合成名、ZIP 和依赖状态，按唯一优先级配对根目录 PNG；兼容“角括号/尖括号”“竖屏框/竖屏视频框”等受控别名，歧义仍保持冲突。
- [x] 使用真实“支付宝阿宝”素材副本验证：6 张 PNG、6 个逐合成 ZIP、6 个旧版收集 manifest 形成 6 条可导入记录，12 个文件将导入、6 个 manifest 仅校验、0 个冲突。新增 1 项扫描回归测试，全项目 87 项自动化测试全部通过。
- [x] 外部收集器增加代表帧预览：每个合成默认选择第 2 秒，短合成回退到最后有效帧；单选合成后可在预览窗口输入秒数、拖动时间轴或前后逐帧刷新。
- [x] 预览通过本机 AE 2025 `aerender` 后台引擎生成，不打开 AE 主界面；使用自带 TIFF+Alpha 模板后在本地无损转为 PNG，并对 AE 预乘 Alpha 执行反预乘。真实“背景”得到 1920×1080 RGB PNG，真实“人名条”得到 500×300 RGBA PNG，Alpha 范围 0–255。
- [x] 收集完成后同名 PNG 与 ZIP 位于同一输出目录，`preview_file`、时间与帧号写入 `xbot-collection` 并刷新 ZIP 内 manifest。真实“阿宝包装.aep / 背景”临时闭环生成 21,412,174 字节 ZIP 和第 50 帧 PNG，Eagle 扫描为 1 条 ready、2 个将导入文件、0 冲突。
- [x] Eagle 扫描器优先使用收集 manifest 显式 `preview_file`，仍兼容旧版名称匹配；修复子目录依赖 PNG 被误标为冲突的问题。全项目 93 项自动化测试全部通过。
- [x] 已重新构建 `XbotAepCollector.exe`（29,107,983 字节，SHA-256 `895B842F0DD2E9A4E763E3E663341A1CD66D4CD4849CC1DEEB5FA07EC0AE3A15`），隐藏启动 6 秒存活检查通过，测试父子进程已全部关闭。
- [x] 增加可选 `XbotPreviewBridge.jsx` 常驻 AE 快速预览桥接：打开同一 AEP 时用 `saveFrameToPng` 快速调帧，按 PNG 完整尾标等待异步写入；桥接离线、工程不一致或超时自动回退 `aerender`，最终入库 PNG 不直接复用快速快照。
- [x] 视频框、横屏框和竖屏框默认推荐唯一合适的上层包装展示合成作为预览来源；用户可在预览窗口选择自身或直接父合成。收集目标仍是原视频框，manifest 新增预览来源合成、关系和渲染器字段。
- [x] 真实“阿宝包装.aep”验证：`视频框`（ID 293）和`竖屏框`（ID 408）均自动推荐父级`包装`（ID 17）；1920×1080 连续快速快照为 2.15–2.25 秒，高质量 `aerender` 为 13.32 秒，交互调帧约快 6 倍。
- [x] 真实“视频框”完整收集得到 2 个依赖合成、5,286,411 字节 ZIP 和取景`包装`第 50 帧的 PNG；manifest 与 ZIP 内清单均记录 `source_composition: 包装 / parent-display`，Eagle 扫描为 1 条 ready、2 个将导入、0 冲突。全项目 97 项自动化测试通过。
- [x] 已构建新版 `XbotAepCollector.exe`（29,119,924 字节，SHA-256 `4F4562C1445AB0C4CA7150E49755DF95579B6D138FC1C2310837A79A3C60B672`），同时输出带 UAC 自提升安装脚本的 `AE-Preview-Bridge` 安装包；EXE 隐藏启动 6 秒存活检查通过。
- [x] `aerender` 高质量预览优先调用用户自定义的 `Xbot PNG with Alpha` 输出模块直接生成 PNG，只接受包含正确 PNG 签名和 IEND 尾标的完整文件；模板缺失或输出失败时自动回退现有 TIFF+Alpha 中转。
- [x] AE 完全关闭时使用真实“阿宝包装.aep”验证：`Xbot PNG with Alpha` 被 `aerender` 正确识别，`包装` 1920×1080 RGBA PNG 直接输出用时 12.36 秒、大小 4,901,244 字节；`人名条` 500×300 RGBA PNG 用时 10.42 秒、Alpha 范围 0–255。
- [x] 新增直接 PNG 与 TIFF 回退回归测试，全项目 99 项自动化测试通过；新版 `XbotAepCollector.exe` 为 29,119,138 字节，SHA-256 `52C9C50CF767134EC4CE65B0F795A4350F6D193084B0D16ED1552B48A15AB656`，隐藏启动 6 秒存活检查通过，父子测试进程已全部关闭。

## 当前未完成

- [x] 真实 AE 主程序验收：在 AE 2025 中打开“阿宝包装.aep”收集工程，覆盖“背景”“包装”“人名条”“视频框”“竖屏框”；表达式未见报错，收集工程内素材链接正常，最终画面与预览来源一致。字体和第三方效果未出现阻止性缺失；已有依赖警告继续保留在 manifest/描述中，接收环境仍需人工确认。
- [x] 外部 AEP 收集器支持选中合成一键查看直属预合成，并将直属预合成逐个独立收集、生成 PNG/manifest/ZIP；每个子包继续递归收集自己的下层预合成和素材，不把上层包装合成混入导出结果。新增核心查询/导出接口、GUI 操作和 CLI 入口，20 项 Python 收集器测试通过。
- [x] 修复视频框父级预览取帧：找到视频框合成在父级中的图层 `in_point`，默认渲染图层出现后的第 2 秒；若超出图层可见范围则取最后一个可见父级帧，并在 manifest 记录父级图层时间和实际取样偏移。新增 3 项 Python 回归测试。
- [ ] 解析 AE Collect Files Report 的正文内容；当前第一版保留 Report 文件名，并直接从工程扫描字体、效果和缺失素材。
- [x] 重复导入三选项与批次记录模型：按 `batch_id`、稳定 `batch_key`、源路径或 manifest 标识检测重复；默认先停下，用户可选择复用、更新或新建独立批次，不静默生成重复素材。新增 `batches`/`batch_details` 迁移、6 项导入模式回归测试和 3 项数据库测试；当时验证为 93 项 JS + 18 项 Python 测试全部通过。
- [x] 插件正式入库结果同步：插件把 `batch_id`、`package_id`、PNG/ZIP Eagle ID 和批次元数据追加到共享 JSONL 队列；bot 作为 SQLite 唯一写入方，按 `event_id` 幂等记录批次并强制刷新 Eagle 索引，失败事件可重试。离线链路覆盖重复消费、刷新失败重试和结果落库；真实 Eagle UI 与常驻 bot 的现场联调仍需单独执行。

## 当前里程碑：M5 入库助手插件（提前实施）

完成条件：

- 插件可在真实 Eagle 库（`E:\Eagle资源库\包装.library`，需先把 Eagle 切到包装库）完成“选择本地项目包装文件夹 → 扫描预检 → 确认导入 `00_待入库` 批次”。
- 重复拖入同一文件夹时提供复用、更新或新建三个选择，不静默生成重复素材（逻辑与自动化测试已完成，待真实 Eagle 界面复验）。
- 插件可对 `00_待入库` 批次执行正式入库：按类型归位到 `01_预览图`/`02_AE源文件` 并提示空批次手动清理（已通过真实 Eagle 手动导入批次验证）。
- SQLite 索引写入与 bot 磁盘同步链路已实现并完成离线联调；真实 Eagle UI → 常驻 bot 现场验证待补。

## 后续里程碑：M2 版本与模糊输入

完成条件：扩充真实项目素材（如阿宝），支持通用包装、多项目复用和项目修改版关系；完善错别字、别名和近义词匹配；补充权限、入库校验和异常恢复。

建议实施顺序：

1. 按现有入库规则补充阿宝等真实项目的包装素材，验证多项目、多类型检索。
2. 建立通用包装与项目修改版关系，完善 `base_package_id` 版本链。
3. 扩展错别字、别名和近义词匹配，补充异常恢复用例。

## 已知风险与待确认项

- `FeishuBot` 登录自动启动脚本和单实例保护仍保留，但当前 Windows 计划任务未注册；X.bot 已于 2026-09-03 手动恢复在线，电脑重启后的自动上线仍待重新配置。
- 既有 X.bot 的运行资源和日志仍位于 `C:\Users\ADMIN\Documents\飞书`；代码主版本已迁入本仓库，通过 `LARK_BOT_RUNTIME_ROOT` 复用这些本机资源。
- `视频框` ZIP 为 64,715,970 字节，超过飞书机器人单文件上限，已实现并实测云盘链接降级；云盘容量与流量受企业飞书套餐约束。
- 当前真实 `变速箱包装` 手动导入批次已经正式归位：信息条、视频框、背景各 1 组 PNG+ZIP，共 6 个正式素材；配对、标签和描述经当前 Eagle Web API 复核一致。空的批次文件夹与 `Root_Collected_Projects` 仍需用户在 Eagle 界面手动删除。
- Eagle 4.0 Web API V2 和当前 Folder Plugin API 没有文件夹删除方法；空批次清理暂时需要用户在 Eagle 界面完成，自动删除必须等待官方 API 支持或安全的插件能力。
- 外部 AEP 工具能列出全部合成和依赖关系，但不能自动判断哪些合成值得复用；包装合成仍需人工选择和分类。
- AE 的 `Reduce Project` 可能移除被表达式跨合成引用的内容，提取工具必须只处理副本并将这类引用列为人工复核项。
- 外部工具会在内存工程中执行依赖精简并只保存到新目录；跨合成表达式可能引用未作为图层嵌套的合成，因此必须打开收集工程复核，不能只相信静态依赖图。
- 外部工具的输出已通过再次解析、路径存在性、ZIP 完整性、代表帧渲染、真实 AE 主程序打开验收和 Eagle 扫描检查；Report 正文解析与真实 Eagle→bot 现场联调仍是 M5 的剩余收尾项。
- 预览图不是“纯离线 AEP 解析”：本机必须安装且已授权 After Effects，工具会在后台启动 `aerender` 渲染引擎。默认超时为 180 秒；授权、第三方插件或缺失字体均可能导致渲染失败，收集 ZIP 仍保留但 Eagle 会等待补齐 PNG。
- 快速预览桥接需要用户在 AE 中打开同一个 AEP 并保持 `XbotPreviewBridge.jsx` 面板运行；它只加速交互选帧，不改变离线收集能力。未安装或未连接时继续使用原有后台渲染。
- 字体、第三方效果和示例素材不会因打包而自动获得授权或兼容性，入库前需要展示依赖警告。
- 核心文档存在版本库主版本和外部工作副本，修改后必须同步验证。

- 实测完整包装查询闭环（候选富文本、确认后 ZIP/云盘，含且不含语音）均稳定通过，`replyPost` 与 ZIP 发送链路正常；事件消费者非零退出已由监听器自动退避重试，其他未捕获异常仍交由任务计划重启；`bot/lib/packaging-service.js` 的 `startQueryFromResult` 保留 `packaging dbg:` 逐步日志，便于继续定位异常。
