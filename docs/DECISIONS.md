# 项目决策记录

本文件记录会影响多个模块或长期行为的决策。需求发生反复时，不删除旧记录；把旧决策状态改为“已取代”，并新增替代决策。

## 状态定义

- **有效**：当前必须遵循。
- **试行**：正在验证，可在复盘后调整。
- **已取代**：保留历史原因，但不再执行。
- **废弃**：确认不采用。

## D-001：项目名优先检索

- 日期：2026-08-18
- 状态：有效
- 决策：检索优先级以项目名称精确匹配和项目别名匹配为先，图片相似度只作为辅助召回方式。
- 原因：同一包装可能被多个项目复用，发送人通常记得项目名，但不一定知道准确包装名称。

## D-002：预览后确认再发送源文件

- 日期：2026-08-18
- 状态：有效
- 决策：机器人先返回 PNG 候选，只有原查询人回复候选消息确认后才发送 ZIP。
- 原因：降低模糊匹配、版本并存和群聊误确认导致的误发风险。

## D-003：Eagle 存素材，SQLite 存稳定映射

- 日期：2026-08-18
- 状态：有效
- 决策：Eagle 保存预览图和源文件；SQLite 保存项目、包装、版本、Eagle ID、源路径和 AE 合成名称之间的关系。
- 原因：文件名、位置和同名关系可能变化，不能作为唯一关联依据。

## D-004：源文件统一使用 ZIP

- 日期：2026-08-18
- 状态：有效
- 决策：发送包含 AE 工程及依赖素材的 ZIP，不发送裸 `.aep`。
- 原因：避免工程打开后缺少外部依赖。

## D-005：新查询必须明确提及机器人

- 日期：2026-08-18
- 状态：有效
- 决策：私聊和群聊中的新包装查询都必须明确 `@X.bot`；进入候选确认阶段后，原查询人回复时不需要再次提及。
- 原因：降低机器人噪音和群聊误触发。

## D-006：待入库目录按项目组织

- 日期：2026-08-19
- 状态：有效
- 决策：`00_待入库` 下每个项目建立一个文件夹，项目文件夹内同时放置该项目的 PNG 预览和 ZIP 源文件。
- 原因：项目是第一检索入口，同项目素材集中处理更符合实际入库习惯。

## D-007：Eagle 入库助手放到第五阶段

- 日期：2026-08-19
- 状态：有效
- 决策：Eagle 入库助手采用窗口插件形态，在第五阶段实现；插件负责入库和索引同步，不替代 X.bot 飞书服务。
- 原因：先完成文字检索闭环，再用已稳定的数据规则固化插件，减少过早自动化带来的返工。

## D-008：Git 中的核心文档为主版本

- 日期：2026-08-19
- 状态：有效
- 决策：`docs/飞书机器人包装素材检索项目文档.md` 为受版本控制的主版本，外部同名文档作为工作副本同步维护。
- 原因：长期项目需要可追溯的变更历史，同时保留用户原有文档使用位置。

## D-009：Eagle 数据只能通过官方 API 修改

- 日期：2026-08-19
- 状态：有效
- 决策：使用 Eagle Plugin API 或 Web API 修改目录、标签和素材，不直接编辑 `.library` 内部元数据文件。
- 原因：避免破坏资源库内部一致性，并保留清晰的操作边界。

## D-010：AE 包装提取是 Eagle 入库的前置环节

- 日期：2026-08-19
- 状态：有效
- 决策：第五阶段增加 AE 包装提取辅助工具。用户在 AE 中明确选择可复用合成后，工具在工程副本或收集副本上生成静态 PNG 预览、包含依赖的 ZIP、AE 收集报告和标准化 manifest，再交给 Eagle 入库助手审核。工具不得直接精简、改名或覆盖原工程。
- 原因：真实项目工程同时包含包装合成、业务画面、示例素材、字体和第三方效果。直接把项目目录导入 Eagle 会产生重复文件和错误映射；先在 AE 侧按合成提取和校验，才能得到可复用、可追溯的入库单元。

## D-011：本地项目文件夹对应一个待入库批次（已取代）

- 日期：2026-08-19
- 状态：已取代（由 D-045 取代）
- 决策：用户拖入或选择一个本地项目包装文件夹时，Eagle 入库助手在 `00_待入库` 下创建一个对应的批次文件夹。插件递归扫描源目录，但优先按 manifest 明确列出的路径导入；没有 manifest 时只识别符合待入库规则的预览 PNG 和源文件 ZIP。展开的 `.aep`、业务视频、截图、依赖图片和 Report 只用于校验，不原样镜像到 Eagle。每次导入生成稳定的 `batch_id`，重复导入必须提示复用、更新或新建批次，不能静默产生重复素材。
- 原因：按文件夹保留项目上下文更符合制作人员的交付习惯，但原样递归导入会把收集工程的展开目录、业务素材和 ZIP 同时导入，造成重复、噪声和误发风险。

## D-012：Eagle 入库不复核已打包 ZIP 内依赖的必要性

- 日期：2026-08-19
- 状态：有效
- 决策：依赖素材是否需要进入 ZIP，由上游 AE 包装提取和打包环节负责。对于已经打包完成的 ZIP，Eagle 入库环节只校验文件存在、可读取、格式正确，并能与唯一的 PNG 和包装记录配对；不在此环节解包或逐项判断 ZIP 内视频、截图、图片等依赖是否必要。字体和第三方效果继续作为依赖事实和警告记录。
- 原因：重复判断 ZIP 内部素材会把上游提取职责带入 Eagle 审核，既增加人工成本，也可能误删合成真实依赖。入库环节应专注于分类、配对、元数据和可追溯性。

## D-013：正式素材描述只记录事实字段

- 日期：2026-08-19
- 状态：有效
- 决策：Eagle 正式素材描述记录项目、原 AE 项目、`package_id`、包装名称、类型、版本、AE 合成、`batch_id`、配对 Eagle ID、状态、字体和第三方效果等稳定事实。用户确认、审核过程、ZIP 校验边界和操作历史不写入素材描述。
- 原因：描述用于日常检索和追溯，混入一次性流程话术会制造噪声，并让同一包装的 PNG 与 ZIP 元数据难以长期保持一致。

## D-014：正式入库后允许清理空批次文件夹

- 日期：2026-08-19
- 状态：有效
- 决策：正式素材已从待入库批次移入分类目录后，可以删除空的批次文件夹及空子目录。删除前必须递归确认目标文件夹直接素材数、后代素材数和所有子目录内容均为空；任何一级不为空都必须终止。当前官方 Web API 和 Folder Plugin API 不提供删除方法时，保留空文件夹并由用户在 Eagle 界面手动删除，不得直接修改资源库元数据。
- 原因：清理空批次可以避免 `00_待入库` 长期积累无效目录，同时递归空目录检查和官方接口边界能防止误删仍有素材的批次。

## D-015：复用既有 X.bot 监听器并将包装检索作为独立模块接入

- 日期：2026-08-19
- 状态：有效
- 决策：既有 X.bot 的 Node 事件监听、群聊 `@mention` 门控、大模型、周报和语音能力继续保留；代码主版本迁入本仓库，包装检索以独立的 Eagle 同步、SQLite、意图解析和确认状态模块接入同一事件消费者。运行时可通过 `LARK_BOT_RUNTIME_ROOT` 继续复用原飞书目录中的回复规则、语音资源和日志，避免同时启动第二个事件消费者。由于 `lark-cli event consume` 的 compact 消息事件不含回复关系，确认阶段使用 bot 身份读取当前消息详情中的 `reply_to`，再校验原查询人和候选消息。Eagle 数据只通过官方 API 读取元数据；原文件路径依据官方返回的当前库路径和稳定 Eagle item ID 只读解析并写入 SQLite，不修改 `.library` 内部文件。
- 原因：新建独立消费者会造成重复收消息和回复，重写既有机器人又会破坏已经验证的能力。模块化接入能保留现有行为，并让未来 Eagle 插件提供 `filePath` 时只替换同步适配层。

## D-016：索引同步可只读解析指定 Eagle 库目录

- 日期：2026-08-19
- 状态：有效
- 决策：包装索引同步支持直接只读解析指定 Eagle 库目录（库根 `metadata.json` 与 `images/*.info/metadata.json`），通过 `LARK_BOT_EAGLE_LIBRARY_PATH` 指定，默认 `E:\Eagle资源库\包装.library`。该模式不依赖 Eagle 当前活动库，也不修改 `.library` 内部任何数据；Eagle 界面可继续停留在其他库。
- 原因：Eagle Web API 只能访问当前活动库，而用户需要在保留当前 Eagle 界面库的同时让 bot 检索包装库。

## D-017：超限源文件走飞书云盘链接降级

- 日期：2026-08-19
- 状态：有效
- 决策：超过飞书即时消息单文件上限（约 30MB，bot 上传接口拒绝）的源文件，确认后由 bot 上传到自身飞书云盘根目录，并在原查询消息下回复下载链接；不拆分、不压缩源文件。小文件仍直接发送。
- 原因：机器人上传接口对单文件大小限制远小于用户通道；实测 61.7MB ZIP 上传云盘成功且链接可下载。

## D-018：多条候选查询并存，语境延续不取消旧查询

- 日期：2026-08-19
- 状态：有效
- 决策：新查询和语境延续（如“看看背景”）都不取消已存在的待确认查询；多条查询并存，各自保留 20 分钟，回复任一旧预览仍可确认对应候选（候选级幂等保证已发送的不重复发送）。只有 20 分钟过期或用户明确拒绝（“不是/都不对”）才使查询失效。
- 原因：用户会在已看到的候选基础上继续换类型查看，旧预览突然失效会导致“已发送或待确认候选无法再回复”的体验问题。

## D-019：包装查询触发采用规则快路径 + 大模型语义兜底

- 日期：2026-08-19
- 状态：有效
- 决策：包装查询触发分为两层：规则层要求项目名（或别名）命中且带动作词或领域词其一；规则未命中但消息已 `@X.bot` 时，由大模型判断是否为包装查询并提取项目与类型。`@X.bot` 门控在群聊与私聊均保留；AI 不可用或判断失败时自动回退规则，不拦截普通聊天。
- 原因：纯关键词规则无法覆盖自然说法，而完全放开触发会造成群聊误触发；双层判断在灵活性与低噪音之间取得平衡。

## D-020：入库助手插件采用“窗口插件 + 纯逻辑扫描引擎 + Eagle API 导入层”

- 日期：2026-08-20
- 状态：有效
- 决策：Eagle 入库助手插件拆为三部分：`eagle-plugin/lib/scan.js` 提供可单测的扫描/配对/四状态/批次逻辑；`eagle-plugin/lib/eagle-api.js` 负责 Eagle Plugin API 适配与正式写入；`index.html`/`js/plugin.js` 只做窗口交互。新导入在配对、命名和校验通过后直接写入 `01_预览图/<类型>` 与 `02_AE源文件/<类型>`，并按 D-013 写事实字段描述、回写配对 Eagle ID。`00_待入库` 只作为历史兼容路径保留；重复导入三选项和扫描结果中的 `batchKey` 继续用于直接入库检测。
- 原因：把扫描、配对与批次规则固化成纯模块，可以先用自动化测试验证规则，再接入真实 Eagle 环境；一次性实现正式归位和索引同步会引入较多耦合，不利于回滚。

## D-021：导入与入库分离为两个独立操作（已取代）

- 日期：2026-08-20
- 状态：已取代（由 D-045 取代）
- 决策：历史版本的插件提供两个独立按钮。`导入` 负责把本地项目包装文件夹扫描并导入 `00_待入库/<项目>包装` 批次；`入库` 读取用户选择的待入库批次并按 5.7 规则正式归位。该决策仅保留为旧版本行为记录，新版本由 D-045 取代。
- 原因：当时需要把扫描搬运和正式归位拆开，以便先在待入库批次中统一检查；现已通过同一界面的确认闸门保留检查能力，并移除中间落点。

## D-022：历史待入库兼容路径不依赖插件导入

- 日期：2026-08-20
- 状态：有效
- 决策：对于升级前已经存在的 `00_待入库` 批次，插件仍可独立读取用户选择的项目批次并递归覆盖其子目录；素材即使由用户直接使用 Eagle 文件夹导入、没有插件注释，也要按受控位置、文件名和目录名重新识别 PNG/ZIP、建立候选配对，并允许在预检界面补充缺失的包装类型。只有唯一配对且字段完整的记录才能正式归位；归位时再通过 Eagle Plugin API 写入稳定 ID、配对关系、标签、事实描述和规范名称。
- 原因：历史数据不能因为新流程移除中间落点而失去处理入口；兼容路径仍以 Eagle 中的真实文件为准，不依赖插件导入阶段的注释。

## D-023：标注与人名条默认归入信息条

- 日期：2026-08-20
- 状态：有效
- 决策：Eagle 入库助手在本地文件扫描、手动导入批次扫描和 manifest 解析中统一使用包装类型别名规则。包装名称、文件名、目录名、标签或 manifest 类型包含“标注”或“人名条”时，默认把包装类型规范化为“信息条”；预检界面仍允许用户手动改正。
- 原因：项目分类规则已经把人名条和标注条统一纳入信息条。自动入库应直接执行该规则，避免“小标注”等明确素材反复停在待补充状态。

## D-024：明确的新包装查询优先于历史候选确认

- 日期：2026-08-20
- 状态：有效
- 决策：消息明确 `@X.bot`，并命中已知项目名（或别名）及包装动作词/领域词时，优先按新包装查询处理，不得先关联当前会话的历史待确认记录。候选确认优先依赖飞书 `reply_to`；没有回复引用时，只有纯确认话术或不含项目名的类型追问可以回退最近待确认查询。历史候选过期只影响该候选的确认，不得截断可独立成立的新查询。该决策补充 D-005、D-015、D-018 和 D-019，不改变多查询并存与 20 分钟有效期规则。
- 原因：确认回退本来用于补偿 compact 消息事件缺少回复关系。如果领域词（如“背景”）本身就能触发回退，明确的新查询会被错误绑定到旧候选，并因旧记录过期而提前结束。先识别可独立成立的新查询，可以保留无引用确认能力，同时避免历史状态污染新的检索意图。

## D-025：AE 包装整理采用 ScriptUI 面板与原生 Collect Files 半自动闭环

- 日期：2026-08-20
- 状态：已取代（由 D-026 取代）
- 决策：AE 包装整理第一版采用可停靠的 ExtendScript/ScriptUI 面板。面板读取用户在 Project 面板中明确选择的包装合成，负责逐条元数据、代表帧 PNG、工程静态风险扫描、标准化 manifest、收集目录 ZIP 压缩和交付检查；依赖收集仍打开 AE 原生 `File > Dependencies > Collect Files` 对话框并由用户确认 `For Selected Comps`、输出位置和 Report。第一版默认不自动执行 `Reduce Project`，不修改、改名、删除或覆盖原工程中的项目项。缺失素材、跨合成引用和扫描失败阻止入库；字体与疑似第三方效果作为人工复核警告。
- 原因：After Effects 的 Collect Files 已能可靠生成工程副本、依赖和 Report，而 Reduce Project 会删除非从属合成，跨合成表达式还可能被一并移除。把高风险动作保留在 AE 原生可见流程中，同时自动化重复的元数据、预览、打包和配对工作，能先建立可回滚的真实闭环，再根据项目样本决定是否增加更深自动化。

## D-026：AEP 收集主入口改为外部合成浏览与收集工具

- 日期：2026-08-20
- 状态：试行
- 决策：AE 包装收集主入口改为 Windows 外部工具。工具使用 MIT 许可的公开 `py-aep` 直接读取 AEP，必须在不启动 After Effects 的情况下显示工程内全部合成、父级使用关系和内部预合成；用户明确选择合成后，工具递归保留嵌套合成和图层素材，只在新目录写出精简 AEP、素材目录和 manifest，并在交付前重新解析输出 AEP。BHY Collect Pro 仅作为产品行为参考；由于随包没有源码或许可证，不修改、不复制、不重新分发其 EXE，也不把其私有缓存格式作为稳定接口。旧 ScriptUI 面板保留为历史和代表帧 PNG/AE 内验证能力，不再作为收集主入口。
- 原因：用户需要在不打开 AE 的情况下直接浏览并选择工程内合成，且第三方 BHY 在真实工程中出现“后台识别到 11 个合成、界面缓存却为空”的同步问题。基于公开依赖重做可以把“合成必须完整显示”和“收集结果必须自检”固化为可测试能力，同时维持不覆盖原工程、跨合成表达式需人工复核的安全边界。

## D-027：每个合成收集后生成独立 ZIP

- 日期：2026-08-20
- 状态：有效
- 决策：外部 AEP 收集工具对每个用户选中的合成分别生成一个展开收集目录和一个同名 ZIP。ZIP 内以该收集目录名称作为唯一顶层目录，包含精简 AEP、复制并重链接的素材和 manifest；manifest 的 `source_file` 必须记录实际 ZIP 文件名。工具必须在报告成功前检查 ZIP CRC，并确认 AEP 与 manifest 均存在；压缩或校验失败时删除该次收集目录和半成品 ZIP，不把不完整产物交给 Eagle。展开目录继续保留用于 AE 打开验收，Eagle 只把同级 ZIP 视为源文件候选。
- 原因：Eagle 的正式入库和 X.bot 源文件发送都以 ZIP 为稳定交付单元。逐合成自动打包能避免人工压缩时把多个包装混在一起或漏掉依赖，同时保留展开目录便于在真实 AE 中复核画面和链接。

## D-028：收集记录与正式入库 manifest 使用不同类型

- 日期：2026-08-20
- 状态：有效
- 决策：逐合成收集器生成的 manifest 使用 `manifest_type: xbot-collection`，只记录合成、对应 ZIP、依赖和收集自检事实；补齐项目、包装、类型、版本、PNG 配对和稳定 ID 的正式入库清单使用 `manifest_type: xbot-eagle-ingest`。Eagle 插件只有在发现正式入库清单时才启用“仅导入 manifest 明确引用文件”；发现收集记录时，读取 `composition.name`、`source_file` 和依赖状态，并按唯一优先级与根目录 PNG 配对。无法唯一配对时必须保持冲突，不允许猜测。旧版未带 `manifest_type` 的收集记录通过 `collector_mode: offline-py-aep` 兼容识别，旧版正式清单继续按 `packages` 或 `package_id` 结构识别。
- 原因：收集记录在生成时尚不知道最终预览 PNG、包装类型和版本，不具备正式入库清单的完整字段。把两者混为一种 manifest 会使插件错误进入全局白名单模式，导致本来有效的 PNG 和 ZIP 全部被忽略；显式类型能保留上游事实，同时把最终配对和人工补充留在 Eagle 入库环节。

## D-029：代表帧默认第 2 秒并允许在外部预览窗口调整

- 日期：2026-08-20
- 状态：有效
- 决策：外部 AEP 收集工具为每个合成默认选择第 2 秒的帧；合成短于 2 秒时回退到最后一个有效帧。用户可在工具预览窗口通过时间轴、秒数输入和前后逐帧重新渲染；只有当前时间已生成真实图像时才能确认。真实画面使用本机已授权 AE 的 `aerender` 后台引擎生成，不打开 AE 主界面；透明通道必须保留。最终 PNG 与 ZIP 同名，`xbot-collection` 写入 `preview_file`、秒数和 AE 帧号，Eagle 优先按显式引用配对。
- 原因：第 0 帧常处于动画尚未进入的空白或初始状态，固定生成一张图又无法应对不同动画节奏。默认第 2 秒能减少大多数手动操作，而可见预览和逐帧调整保留了人工选择代表画面的决定权。离线 AEP 解析不能还原插件、字体、表达式和最终合成画面，因此渲染阶段必须显式依赖 AE 引擎。

## D-030：交互预览使用常驻 AE 加速，视频框从上层包装展示合成取景

- 日期：2026-08-20
- 状态：已取代（由 D-040 取代）
- 决策：外部 AEP 收集器的交互调帧优先通过可选 `XbotPreviewBridge.jsx` 与已经打开同一 AEP 的常驻 AE 通信，调用 `saveFrameToPng` 快速生成快照；桥接未运行、工程不一致或超时时自动回退 `aerender`，不得自动切换或覆盖用户当前 AE 工程。快速快照只用于选帧，收集阶段始终只额外执行一次高质量 `aerender` 作为最终 PNG。视频框、横屏框和竖屏框默认使用唯一合适的直接父级包装展示合成取景，使框体与背景一起展示；存在歧义时默认自身并让用户明确选择。预览来源只影响 PNG，实际精简 AEP 和 ZIP 仍以用户选中的视频框合成为根，manifest 同时记录两者。
- 原因：FX Console 的速度来自已启动 AE 内的快照工作流，但没有公开自动化接口，不能作为外部工具的稳定依赖。公开 AE 自动化项目证明“常驻面板 + 本地 IPC + `saveFrameToPng`”可以复用 AE 上下文；把快速快照限制在交互阶段，再由现有高质量渲染生成最终图，可以兼顾速度与 Alpha 质量。视频框自身通常只有透明框体，缺少背景和承载画面时不利于 Eagle 中快速辨认，因此需要把展示语境与实际收集边界分开。

## D-040：视频框预览取父级图层出现后的第二秒

- 日期：2026-09-02
- 状态：有效
- 决策：当视频框、横屏框或竖屏框选择唯一合适的直接父级包装展示合成作为预览来源时，不再固定渲染父级合成的绝对第 2 秒。收集器读取该父级中视频框合成图层的 `in_point`，默认取 `in_point + 2 秒` 的父级画面；若超过图层 `out_point` 或父级时长，则按父级帧率回退到图层仍可见的最后一帧。用户仍可在预览窗口手动调整，manifest 记录父级图层时间、实际取样时间和偏移。
- 原因：视频框图层可能在父级时间线的第 2 秒之后才出现，固定渲染父级 2 秒会得到空白或缺少视频框的预览。以图层实际出现时间为基准可以稳定得到包含视频框的使用语境，同时不改变视频框作为收集根合成的依赖边界。

## D-031：AE 未打开时优先使用自定义输出模块直接生成 PNG

- 日期：2026-08-20
- 状态：试行
- 决策：后台 `aerender` 预览优先调用名为 `Xbot PNG with Alpha` 的输出模块，直接生成 RGB+Alpha、Straight / Unmatted PNG。程序必须检查 PNG 文件头和 IEND 尾标，只在文件完整后才原子替换目标预览。当前 AE 版本没有该模板、模板名不一致或直接输出失败时，自动回退至已验证的 TIFF+Alpha 中转与反预乘流程。不把 PNG 模板设为收集或渲染的硬前置。
- 原因：`aerender -OMtemplate` 只能按已存在的输出模块名称选择格式，不能只靠 `.png` 扩展名稳定设定格式。自定义英文名称避免了 AE 界面语言造成的内置模板名差异，直接 PNG 又能省去 TIFF 落盘、本地反预乘与再编码环节；保留 TIFF 回退则保证工具在新 AE 版本或其他机器上仍可用。

## D-032：候选操作必须回复机器人消息

- 日期：2026-08-21
- 状态：有效
- 决策：包装候选确认、拒绝、数字选择和类型语境延续都必须回复机器人发出的候选预览或提示消息，并通过飞书 `reply_to` 精确关联查询。没有回复引用时不得查询最近一条 `pending` 记录，不得继承项目，也不得发送源文件；消息继续按新查询或普通聊天处理。多条查询仍可并存，回复任一有效候选消息时仍按其独立状态处理。本决策取代 D-018、D-024 中允许无回复引用回退最近待确认查询的部分，其他规则继续有效。
- 原因：飞书消息可能缺少可用于可靠猜测的上下文，同时会话中可能存在多条历史候选。无引用回退会把新的包装查询、普通领域对话或类型词错误绑定到旧查询，甚至返回过期提示。强制回复把用户操作与查询建立明确关系，能消除歧义并简化状态机。

## D-033：候选内容合并为一条富文本消息

- 日期：2026-08-21
- 状态：有效
- 决策：单候选和多候选统一发送一条飞书富文本消息。消息内按“候选编号与名称 → 对应 PNG”的顺序展示，并在末尾写明回复选择方式、有效期和原查询人限制；不再分别发送图片消息和文字提示。该富文本消息 ID 作为查询的唯一候选交互入口，用户回复它进行确认、拒绝或类型语境延续。
- 原因：图片和文字拆成两条消息会让用户不知道应该回复哪条，也会在群聊插入其他消息后破坏名称与图片的对应关系。统一成一个可回复单元能保持信息完整，并与 D-032 的精确回复规则形成一致的用户心智。

## D-034：接收富文本与纯图片时保持群聊低噪音和图片外发边界

- 日期：2026-09-01
- 状态：有效
- 决策：`lark-cli event consume` 输出的 `post` 富文本先把 `@`、可见文字和图片占位规范化，再进入既有 AI 对话、包装查询和候选确认路由；群聊仍要求有效 `@X.bot`，候选操作仍要求回复引用。纯图片在私聊直接回应；在群聊中仅当事件有效提及 X.bot，或图片消息回复的是 X.bot 消息时回应。当前不下载图片二进制、不发送外部 AI、不基于占位符推断图片内容，只明确说明尚未接入视觉识别并引导用户补充文字。
- 原因：`post` 的可见文字已经由事件消费者转换为可读内容，继续拒绝会中断正常群聊；纯图片没有文字可供当前 DeepSeek 路由理解，若对群内所有图片自动回复会制造噪音，若把图片直接发给外部模型又违反图片外发需明确许可的安全边界。分层处理可以先恢复消息交互，同时保留后续在用户授权后接入视觉能力的路径。

## D-035：重复批次导入必须显式选择处理方式

- 日期：2026-09-02
- 状态：有效
- 决策：Eagle 入库助手检测到相同 `batch_id`、稳定 `batch_key`、源路径、manifest 标识或正式目录中已有相同 `package_id` 时，默认暂停导入并要求用户选择“复用现有批次、更新现有批次、新建独立批次”。复用模式不新增已存在的 PNG/ZIP；更新模式复用现有 `package_id` 和 Eagle 素材并刷新批次元数据，缺失的一侧才补导入；新建模式生成独立 `batch_id`，不依赖新建待入库文件夹。历史待入库批次及包装明细通过 SQLite `batches`/`batch_details` 迁移记录，不能依赖文件夹同名静默判重。
- 原因：同一项目文件夹可能被重复拖入，且同一 `package_id` 的版本或元数据可能发生更新。显式三选项能保留用户对复用、更新和并存版本的决定权，避免重复素材污染 Eagle；SQLite 记录批次身份和明细，为后续插件导入结果与 bot 索引联调提供稳定主键。

## D-036：测试采用精简、精准的最小风险覆盖

- 日期：2026-09-02
- 状态：有效
- 决策：后续测试只为新增行为、关键边界和已知回归风险提供最小有效覆盖。已有实现未发生变化时，不重复增加等价用例；测试数量不作为完成指标。优先选择能直接证明行为的单元或小型集成测试，真实接口验证只在离线测试无法覆盖时加入。
- 原因：项目同时包含 bot、Eagle 插件、SQLite 和 AE 工具链，重复测试会放大维护成本并稀释真正的失败信号。以风险和行为为中心可以保持验证精准，同时让测试集随功能变化而增长，而不是随时间堆积。

## D-037：插件通过事件队列交给 bot 单写入 SQLite

- 日期：2026-09-02
- 状态：有效
- 决策：Eagle 插件正式入库后只通过共享运行根目录下的 `packaging-ingest-events.jsonl` 追加批次事件，不直接打开或写入 SQLite。bot 监听器启动时消费事件，使用 `event_id` 在 `batch_sync_events` 中幂等认领；成功后写入 `batches`/`batch_details`，再强制刷新 Eagle 索引，使后续查询立即看到新素材。刷新失败保留失败状态并允许下一轮重试，已完成事件不重复写入。
- 原因：插件与 bot 运行时可能同时操作 Eagle；由 bot 统一写 SQLite 可以避免数据库竞争和两套事实来源，JSONL 队列又能在 bot 短暂不可用时保留正式入库结果。

## D-038：项目名标签单独归入 Eagle“项目”分组

- 日期：2026-09-02
- 状态：有效
- 决策：Eagle 标签管理中新增“项目”分组，项目名标签统一归入该组；当前包括“支付宝阿宝”“华为途灵”“乾崑奕境”“变速箱”。项目标签仍与包装类型、信息用途、画面结构和背景用途标签并列附着在素材上，不改变检索字段或素材关联。Eagle 插件在直接正式入库前，以及处理历史待入库批次时，通过官方标签组 API 确保该分组存在，并增量加入当前项目标签，不覆盖既有分组内容。
- 原因：项目名是第一检索入口，单独分组可以让项目标签集中浏览、减少未分类标签，并保持标签用途边界清晰。

## D-039：选中合成的直属预合成支持查看与逐个独立导出

- 日期：2026-09-02
- 状态：有效
- 决策：外部 AEP 收集器将“预合成”定义为直接作为所选合成图层源的 `CompItem`，按图层出现顺序去重列出。用户可在 GUI 或 CLI 查看这些直属预合成，也可将每个直属预合成分别走现有安全收集链路，生成独立展开目录、代表帧 PNG、manifest 和 ZIP。每个子包继续递归保留其下层预合成和素材，但不把所选父合成或仅用于预览的上层合成加入子包。
- 原因：包装合成往往同时包含信息条、视频框、背景等可复用元素。复用现有按合成收集能力逐个导出，能让用户快速拆出独立素材，同时保持原工程只读、父级展示关系与实际收集边界分离，避免把整套包装重复打包。

## D-041：多终端共享 NAS 采用插件直写 Eagle、中央服务写索引

- 日期：2026-09-03
- 状态：有效
- 决策：面向多个用户共同使用 NAS Eagle 资源库的部署，用户终端上的集成插件负责在本地运行 AEP Worker、生成 PNG/ZIP/manifest，并通过 Eagle 官方 Plugin API 直接写入当前打开的共享 Eagle Library。Eagle API 操作成功后，插件向中央索引服务提交带有 `event_id`、`batch_id` 和 `package_id` 的幂等入库事件。中央索引服务是项目、包装、版本、配对 Eagle ID、源路径和批次状态的唯一数据库写入方；服务端可以暂时使用 SQLite，但任何用户终端都不得通过 SMB/NAS 直接打开该 SQLite 文件。X.bot 通过中央索引服务查询和读取统一映射。本地 outbox 只负责中央服务不可用时的持久化重试，不是共享主数据库。
- 原因：共享 NAS 场景要求素材真正从每个用户终端进入同一个 Eagle Library，不能只把事件交给某台本地 bot；同时，多个终端直接并发访问同一个 SQLite 文件会放大网络锁、断线和一致性风险。把 Eagle 写入和索引写入拆开，可以保留 Eagle 的官方数据边界，并让中央服务统一处理幂等、审计、重试和对账。
- 验证要求：正式发布前必须用目标 Eagle 版本、NAS 协议和实际网络环境测试并发导入、元数据操作、断线恢复、重复事件、Eagle 重启和索引对账。如果共享 Library 的并发写入不稳定，则增加中央导入协调或串行写入闸门。

## D-042：正式入库支持规则命名与逐条人工覆盖

- 日期：2026-09-03
- 状态：有效
- 决策：Eagle 入库插件在唯一 PNG + ZIP 配对通过后，先展示批量规范命名工作台。预览图默认使用 `项目_包装类型_包装名称_版本.png`，源文件默认使用 `项目_包装名称_版本.zip`；用户可以修改规则模板，也可以逐条覆盖任一名称。确认入库前统一校验空名称、Windows 不允许的字符、结尾空格或句点，以及同一正式包装类型目录中的重复名称；校验失败时不得移动任何素材。名称覆盖只通过 Eagle 官方 Plugin API 写入 PNG/ZIP 的 Eagle item 名称，不改变 `package_id`、`batch_id`、配对 Eagle ID、事实描述、源工程或 ZIP 内部文件。
- 原因：上游收集目录和历史素材的命名不一定统一，但正式目录仍需要便于人工浏览的名称。把自动规则和人工覆盖放在“确认正式入库”之前，可以保留效率和可控性，同时避免把可变文件名当作稳定关联键，也避免半批次归位后才发现命名冲突。

## D-043：AEP 收集器以合成结构浏览器作为主界面

- 日期：2026-09-03
- 状态：已取代（主入口由 D-046 取代，结构语义继续沿用）
- 决策：外部 AEP 收集器的 GUI 主视图采用基于 `parent_ids` / `child_ids` 的可展开合成结构浏览器，并保留全部合成平铺列表作为备用视图。结构树是帮助用户理解合成嵌套关系的虚拟浏览结构，不宣称 AE 合成关系是严格文件夹树；同一合成被多个父级复用时显示共享引用，所有勾选状态按稳定合成 ID 全局保持。点击合成行只改变当前检查对象，复选框才代表独立交付选择；选中父级不会级联选中子合成，父级收集仍递归保留子合成作为依赖，直属预合成只有在用户单独选择时才生成独立收集包。搜索、顶层/直属预合成/可能交付/多处复用筛选只改变浏览范围，不改变收集安全边界。
- 原因：完整项目 AEP 通常包含大量业务镜头、测试和临时合成，平面表格要求用户自行在关系列中建立结构心智，定位包装成本高。把“浏览结构”和“独立交付选择”分离，可以提高寻找效率，避免误把依赖批量导出，同时继续遵守 D-026、D-027 和 D-039 的按合成 ID、逐包 ZIP 和直属预合成边界。

## D-046：AEP 收集主入口迁入 Eagle 插件，Worker 与入库流程合并

- 日期：2026-09-03
- 状态：有效
- 决策：Eagle 插件新增 AEP 收集工作台，主入口在同一窗口内完成 AEP 选择、离线合成结构读取、父子关系浏览、筛选、独立交付勾选和单合成预览。插件调用随工具分发的本机 `XbotAepWorker.exe`；Worker 复用现有 `py-aep` 收集核心，在新输出目录生成精简 AEP、依赖素材、代表帧 PNG、`xbot-collection` manifest 和经过 CRC 校验的 ZIP。收集完成后插件直接调用既有扫描/配对预检，再按 D-045 通过 Eagle 官方 Plugin API 写入正式目录。原始 AEP 只读，独立 `XbotAepCollector.exe` 保留为兼容和离线验收入口。
- 原因：外部 Tkinter 收集器和 Eagle 插件之间需要手动切换目录、确认配对和重复处理，容易让用户误以为收集完成就已经入库。把结构选择和产物生成放到 Eagle 插件中，可以让用户在一个确认链里完成“选择合成 → 生成 PNG/ZIP/manifest → 配对预检 → 正式入库”，同时以独立 Worker 隔离 Python/py-aep 运行时，保持插件界面轻量和原工程只读边界。
- 约束：Worker 不修改原 AEP，不直接操作 Eagle Library；插件只能通过 Eagle 官方 Plugin API 写素材和文件夹。预览仍依赖本机已安装且已授权的 After Effects `aerender`，预览失败时保留收集 ZIP，但配对预检必须阻止缺少 PNG 的记录。插件安装包必须同时分发 Worker，开发环境可通过 `XBOT_AEP_WORKER` 指定 Worker 路径。

## D-044：导入阶段自动配对为建议，允许在候选范围内人工覆盖

- 日期：2026-09-03
- 状态：有效
- 决策：Eagle 插件导入包装文件夹时，扫描器先按 manifest、受控目录和文件名自动形成 PNG + ZIP 配对；插件以成对卡片展示预览图和打包文件，点击 PNG 卡片进入原图预览，点击 ZIP 卡片查看文件与配对事实，并允许用户直接修改包装名称或分别更换 PNG、ZIP。人工覆盖只允许选择扫描结果中的候选、已导入候选或冲突候选文件，不把深层依赖文件重新提升为入库素材；应用后必须重新计算缺失、重复引用和一对多冲突，未形成唯一配对时继续阻止直接写入正式目录。人工覆盖不改变稳定的 `package_id`、`batch_id`、原始工程或 ZIP 内部文件。
- 原因：文件名和 manifest 能覆盖大多数常规交付，但历史项目中预览帧、包装名称和 ZIP 文件名经常被人工改动。把自动结果作为默认建议，同时保留受控候选范围内的人工修正，可以减少重复操作，又不放松深层依赖、重复映射和唯一配对边界。

## D-045：新导入在确认闸门后直接写入 Eagle 正式目录

- 日期：2026-09-03
- 状态：有效
- 决策：插件将“导入”和“正式入库”合并为一个连续流程：用户选择本地包装文件夹后，插件扫描 manifest 和受控候选，展示 PNG + ZIP 配对；用户可以修改项目名称、包装名称，或分别更换配对文件。所有缺失、重复引用、未知类型和命名校验通过后，插件直接通过 Eagle 官方 Plugin API 将 PNG 写入 `01_预览图/<类型>`、ZIP 写入 `02_AE源文件/<类型>`，写入事实描述、稳定配对 ID、项目标签和批次事件，不创建新的 `00_待入库` 中间批次。重复导入仍暂停并提供复用、更新、新建独立 `batch_id` 三选项。升级前已有的 `00_待入库` 批次保留“历史待入库”兼容入口，不改变 D-022 的人工导入兼容能力。
- 原因：新导入本身已经在同一界面完成配对、命名和阻止性校验，再写入待入库目录会增加一次无额外价值的人工切换，也容易让用户误以为素材已经正式可检索。直接写入正式目录能缩短操作链，同时保留确认闸门、稳定 ID、重复保护和历史数据兼容。

## D-047：未知项目包装查询返回当前可查项目列表

- 日期：2026-09-04
- 状态：有效
- 决策：消息已明确表达包装查询意图，但项目名或别名未命中当前 Eagle 索引时，机器人先用自然语言说明当前不在库内，再从刷新后的活动包装目录中提取正式项目名并回复列表，同时给出一条查询示例；不创建候选确认记录，不发送预览或源文件。用户回复该提示并追问“有哪些项目”等目录问题时，直接返回同一份项目列表，不转入普通闲聊。列表只包含当前存在可用包装的项目，不重复展示别名。若目录为空，则明确提示暂时没有读取到可用项目。
- 原因：用户往往知道要调取包装但还不知道库内使用的项目名。直接展示当前可查范围能让用户下一条消息完成项目名补充，同时保留项目名优先、预览后确认和 Eagle 索引事实边界。

## D-048：共享引用按层级实例选择，收集按合成 ID 去重

- 日期：2026-09-04
- 状态：有效
- 决策（取代 D-043 中“勾选状态按合成 ID 全局保持、共享引用不可重复选择”的交互部分）：AEP 结构树中的每个父子路径实例都允许独立勾选，包括 `LINK` 共享引用；界面记录用户实际选择的层级位置，并在收集前明确提示同一合成被多个层级重复选择。确认后，插件、Worker 和收集核心均按稳定合成 ID 保持首次出现顺序自动去重，每个合成只生成一份独立收集包；父级递归依赖仍只在各自包内保留，不因多个层级选择而重复生成。
- 原因：共享引用是合成图中的真实复用关系，禁用后续引用会阻止用户从当前可见层级表达交付意图，也会在首次节点被折叠或筛选隐藏时造成“只有 LINK、无法选择”的误导。将层级选择与交付去重分离，既保留用户的结构上下文，又把重复产物控制交给收集闸门。

## D-051：正式包装维护区分文件修订与新版本

- 日期：2026-09-04
- 状态：有效
- 决策：Eagle 插件新增“已入库管理”工作区。仅替换预览 PNG 或重新生成 ZIP 时，先通过官方 Plugin API 写入新 item，再切换当前配对 Eagle ID，并把旧 item 移入 `03_历史版本/<类型>`；原 `package_id` 和版本保持不变，事件队列和中央索引记录 `revision_id`、旧/新 Eagle ID、操作类型和时间。动画、版式或内容发生变化时，必须创建新的 `package_id` 和递增版本，并通过 `base_package_id` 建立版本链。新增包装则创建独立 `package_id` 和 `v01`，沿用同一配对、命名和校验流程。
- 原因：直接覆盖素材会破坏回滚和追溯，也可能在 PNG/ZIP 更新不同步时形成半更新记录。把“文件修订”和“设计版本”分开，可以让 bot 默认使用当前有效素材，同时保留历史版本和明确的版本继承关系。

## D-052：包装类型配置集中维护并扩展首批类型

- 日期：2026-09-04
- 状态：有效
- 决策：包装类型由共享配置集中维护，首批包含信息条、视频框、背景、分镜排版、分Part板、报道和时间轴；扫描器、manifest、Eagle 文件夹、插件下拉框和 X.bot 意图识别统一使用该配置及其别名。`时间线` 归一化为 `时间轴`，`分P板` 归一化为 `分Part板`，`报道板` 归一化为 `报道`。
- 原因：类型散落在多个模块会导致“能入库但搜不到”或“能搜索但无法入库”的不一致。集中配置能让新增包装类别沿同一条链路生效，并保留人工修正入口。

## D-049：预览按合成 ID 隔离同名合成

- 日期：2026-09-04
- 状态：有效
- 决策：After Effects `aerender -comp` 只能按名称定位时，预览链路必须以用户选中的稳定合成 ID 为准。若工程内存在同名合成，工具在临时副本中仅重命名目标合成，再用唯一临时名称渲染；原始 AEP、收集合成和层级引用保持不变。渲染日志记录目标 ID 与隔离动作，无法解析或保存临时副本时阻止本次预览并保留明确错误。
- 原因：真实 AEP 可能同时存在多个同名背景或包装合成，直接把名称传给 `aerender` 会渲染到错误对象，且仅改调用方名称无法消除 AE 内部歧义。临时副本隔离把 ID 身份转换成可验证的唯一名称，同时不触碰用户原工程。

## D-050：仅图片背景可入库但不进入源文件确认

- 日期：2026-09-04
- 状态：有效
- 决策：扫描到“背景”类型且只有 PNG、没有对应工程 ZIP 时，允许登记为 `sourceOptional` 的正式包装。Eagle 正式目录只写入 `01_预览图/背景` 下的 PNG，注释明确记录“源文件：无（仅图片背景）”，SQLite 中 `source_eagle_id` 与 `source_path` 为空。X.bot 查询命中此类记录时发送预览并明确说明没有工程 ZIP，不创建候选确认记录，也不进入源文件发送流程；信息条、视频框和分镜排版仍因缺 ZIP 阻止入库。
- 原因：部分背景本来就是一张静态图片，强制虚构 ZIP 会污染索引并让用户误以为可以获取工程源文件。将例外限定在背景类型，既保留图片检索价值，又维持“有源文件才确认发送”的安全边界。

## D-053：视频框收集将视频依赖静态化为首帧

- 日期：2026-09-04
- 状态：有效
- 决策：收集目标或其递归依赖中存在“视频框 / 横屏框 / 竖屏框 / 画面框 / 视频画框”合成时，凡被这些合成直接引用的视频素材，必须在收集阶段使用本机可配置的 `ffmpeg` 解码第 0 帧为 PNG，并在精简 AEP 中将原 `FootageItem` 替换为该 PNG。生成的首帧写入收集目录的 `素材/Images`，原视频不得复制到 `素材/Video` 或 ZIP；manifest 写入 `video_frame_replacements`，记录素材 ID、原文件、替换文件和帧号。找不到 ffmpeg、源视频缺失、首帧提取失败或工程重链接失败时，阻止本次收集并删除不完整产物，不得回退为继续打包视频。
- 原因：视频框作为可复用包装交付时只需要一个可确认的静态画面，直接携带业务视频会显著增大 ZIP、引入版权/路径依赖和跨机器解码差异。把首帧替换固化在收集副本中，可以保持原工程只读，同时让交付工程打开即有确定画面；严格失败闸门则避免 manifest 声称已静态化而实际仍依赖视频。

## D-054：Eagle 插件采用任务首页与渐进式单流程导航

- 日期：2026-09-10
- 状态：有效
- 决策：Eagle 插件启动时先显示任务首页，把“新增包装”和“维护已入库包装”作为两个独立业务任务。新增包装入口同时接收单个 AEP 与交付文件夹：AEP 自动进入 Worker 结构读取和合成选择，文件夹自动进入 PNG + ZIP 扫描与配对确认；流程页只显示当前步骤需要的输入、状态和主操作，并持续保留返回导航、三阶段位置和固定底部操作坞。历史 `00_待入库`、批次记录和系统诊断保留在次级菜单，不占用主界面。该导航变化不修改 D-044、D-045、D-046、D-048 和 D-051 的配对、入库、Worker、共享引用及修订边界。
- 原因：同时平铺 AEP 收集、入库工作台、已入库管理、历史和诊断会让用户先理解内部模块，再决定自己要完成的任务。以业务任务起步并按输入类型自动路由，可以减少一次手动切换，同时保留所有安全闸门、真实状态和兼容入口。
