# 项目上下文速览（Context）

> 供后续迭代先读的**轻量摘要**。完整内容仍以各权威文档为准；
> 只有当改动命中对应主题时，才需要回去读全文。保持本文件短、常更新。

## 这是什么项目
X.bot 包装素材检索：在飞书里按项目名/别名/包装类型/标签检索 Eagle 素材。
机器人先回复静态 PNG 候选，原查询人在候选消息下回复确认后，才把对应 AE 源文件 ZIP 发回原消息。

## 当前状态（以 docs/STATUS.md 为准）
- 阶段：M1「文字检索闭环」已完成并对外稳定；M5「Eagle 内 AEP 收集 + 入库工具链」已完成真实 AE 主程序验收、重复导入三选项、Collect Files Report 解析、插件→SQLite→bot 索引联调和 CI，正在收尾中央索引服务与真实 Eagle→bot 现场验证。
- 核心文档版本 **v1.31**；软件版本 **0.0.0**（开发版，未发标签）。Eagle 插件窗口版本为 `1.5.10`，已接入“AEP 收集 / 入库工作台”并列主标签、真正相对插件窗口固定的底部操作栏、预览优先的 PNG + ZIP 配对卡片、卡片内包装类型补充/修改、类型驱动的卡片配色与列表排序、原图/ZIP 信息查看、实时更换预览、受控人工覆盖、项目标签组自动同步、确认后直接写入正式目录，以及 AEP 收集可随时取消、按实际合成事件显示进度和两处统一的 Eagle 风格预览大图交互。
- Eagle 库 `E:\Eagle资源库\包装.library`；包装索引 4 项目、19 组（乾崑奕境 5、支付宝阿宝 6、华为途灵 5、变速箱 3，配对错误 0）。
- 最近焦点：Eagle 插件已内置 AEP Worker，可在同一窗口完成 AEP 结构读取、合成预览、独立选择、PNG/ZIP/manifest 收集和配对预检；结构树现已用 `ROOT` / `PRE` / `LINK`、动态缩进和连接线明确表达根合成、直属预合成与共享引用，保留旧 `00_待入库` 批次兼容入口；独立 AEP 收集器仍支持直属预合成查看/逐个导出并修正视频框父级取帧时间。多终端共享 NAS 的“插件直写 Eagle、中央服务写索引”架构已确定，但尚未实现中央服务和并发现场验收。

## 里程碑速览
| 里程碑 | 状态 |
| --- | --- |
| M0 项目与数据模型 | 完成 |
| M1 文字检索闭环 | 完成 |
| M2 版本与模糊输入 | 下一步（未开始） |
| M3 图片识别 | 后续辅助召回，缺人力确认才可用 |
| M4 运行保障 | 部分（幂等、异常恢复持续补齐） |
| M5 外部 AEP 收集 + Eagle 入库工具链 | 提前实施，真实 AE 验收、重复导入、Report 解析、单机 SQLite 联调和 CI 完成；共享 NAS 架构已确定，剩余中央服务与现场验证 |

## 模块地图
- `bot/` X.bot 监听器、Eagle 索引同步、包装检索服务（`lib/packaging-service.js` 是候选→确认→发送核心）。
- `aep-collector/` 第五阶段 AEP Worker 与兼容外部工具：不启动 AE 主界面，直接解析 AEP，按选中合成安全收集、生成独立 PNG/ZIP + manifest（Windows，`XbotAepWorker.exe` / `XbotAepCollector.exe`）。
- `ae-tool/` 已取代的 AE ScriptUI 方案，其可复用预览/manifest 代码仍保留。
- `eagle-plugin/` 第五阶段 Eagle 收集与入库助手插件（窗口工作台 + 本机 AEP Worker 桥接 + 扫描引擎 + API 导入层）。
- `migrations/` SQLite 迁移；`tests/` 自动化测试。

## 关键决策指针（详见 docs/DECISIONS.md，标题即结论）
- 检索与召回：D-001、D-015、D-016、D-019、D-024、D-047
- 消息与确认：D-002、D-004、D-005、D-017、D-018、D-032、D-033、D-034
- 数据边界：D-003、D-008、D-009
- 入库/工具链：D-006、D-007、D-010、D-011、D-012、D-013、D-014、D-020、D-021（已取代）、D-022、D-023、D-035、D-037、D-038、D-041、D-045、D-046
- AEP 收集与预览：D-025（已取代）、D-026、D-027、D-028、D-029、D-030（已取代）、D-031、D-039、D-040
- Eagle 入库命名：D-042
- 测试策略：D-036
- 变迁注意：D-025 已被 D-026 取代；历史决策只标「已取代」不删除。

## 数据与安全边界（必须遵守）
- 只能经 Eagle API 改数据，绝不直接改 `.library` 内的 `metadata.json`。
- Eagle 存素材；单机 SQLite 或多终端中央索引服务管理项目/包装/版本/预览/源文件稳定映射；不靠同名文件建立唯一关系。
- 多终端插件只能通过 Eagle 官方 Plugin API 写共享 Library，中央服务是共享索引的唯一数据库写入方；客户端不得通过 SMB/NAS 直接打开 SQLite。
- 不入库也不提交：`.library`、`.aep`、ZIP 源文件、SQLite 运行库、日志、密钥、API Token。
- 图片发外部 AI 前必须用户明确许可；机器人发源文件前必须原查询人确认且幂等。
- 源文件始终发 ZIP（含工程与依赖），不发裸 `.aep`。
- 测试遵循精简、精准原则，只覆盖新增行为、关键边界和回归风险。

## 常见命令
- 全量 JS 测试：`node --test tests/*.test.js`
- 全部测试（JS + Python）：`npm test`
- 静态检查：`npm run check`
- 同步 Eagle 索引：`npm run sync:eagle`

## 文档定位表（什么时候才读全文）
| 文档 | 内容 | 何时必读 |
| --- | --- | --- |
| `README.md` | 项目概览、仓库边界、本机资源 | 新参与者/首次 |
| `docs/STATUS.md` | 已完成项、未完成项、里程碑、风险 | 涉及交付/状态更新 |
| 核心项目文档 | 需求、数据模型、消息流程、架构、阶段、验收 | 改功能、改流程、定验收 |
| `docs/DECISIONS.md` | 长期架构/边界/流程决策 | 涉及架构、流程、数据/安全边界 |
| `docs/WORKFLOW.md` | 分支、提交、版本、反复管理、交付清单 | 每次迭代收尾 |

## 建议的上下文恢复路径
1. 读本文件（约 5 分钟）掌握全局。
2. 涉及交付/状态 → 读 `docs/STATUS.md` 相应小节。
3. 涉及功能/流程/验收 → 读核心文档对应章节（按目录直接跳）。
4. 涉及架构/数据/安全边界 → 查 `docs/DECISIONS.md` 对应条目。
5. 收尾一律按 `docs/WORKFLOW.md` 交付清单执行。
