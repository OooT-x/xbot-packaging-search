# X.bot 包装素材检索项目协作规则

本项目是长期迭代项目。任何参与者在修改代码、文档或 Eagle 结构前，都必须先恢复当前项目上下文，避免只依据最近一轮聊天做局部判断。

## 必读顺序（快速恢复）

先读轻量速览掌握全局，仅在改动命中对应主题时才翻对应全文：

1. `docs/CONTEXT.md`（速览：状态、里程碑、模块、决策指针、边界、命令）
2. `README.md`
3. `docs/STATUS.md`
4. 命中功能/流程/验收时：`docs/飞书机器人包装素材检索项目文档.md` 对应章节
5. 命中架构/数据/安全边界时：`docs/DECISIONS.md` 对应有效决策
6. `docs/WORKFLOW.md`

## 事实来源优先级

1. 用户当前明确要求
2. 版本库中的核心项目文档
3. `docs/DECISIONS.md` 中状态为“有效”的决策
4. `docs/STATUS.md` 中的当前实现状态
5. 代码、测试和运行日志体现的真实行为

如果文档与代码不一致，先记录差异并确认真实行为；不得默默选择其中一方。

## 每次迭代必须执行

### 开始前

- 检查当前分支、工作区状态和最近提交。
- 区分用户已有修改与本次任务修改，不覆盖无关内容。
- 从 `STATUS` 和核心文档确定本次范围与验收标准。
- 涉及架构、流程、数据边界或安全边界时，先查 `DECISIONS`。

### 实施中

- 一个分支只解决一个清晰主题，工作分支默认使用 `codex/<topic>`。
- 一个提交只包含一个可解释的逻辑变化。
- 优先进行可回滚的小步修改，并保留必要的迁移路径。
- 需求反复时不删除历史结论：将旧决策标记为“已取代”，并链接新决策。
- 测试遵循“精简、精准”约束：只覆盖新增行为、关键边界和已知回归风险；已有行为不重复铺设低价值测试，不为凑数量增加测试。

### 完成前

- 运行与改动范围相符的测试、静态检查或真实接口验证。
- 更新 `docs/STATUS.md`。
- 更新 `CHANGELOG.md`。
- 如果改变了架构或产品规则，更新核心文档与 `docs/DECISIONS.md`。
- 确认没有提交密钥、运行数据库、日志、Eagle 素材或源文件包。
- 保持 Git 工作区干净，并在交付中报告分支、提交和未完成事项。

## 文档同步规则

- 版本库中的 `docs/飞书机器人包装素材检索项目文档.md` 是受 Git 管理的主版本。
- `C:\Users\ADMIN\Documents\飞书\飞书机器人包装素材检索项目文档.md` 是外部工作副本。
- 核心文档变更先修改版本库主版本，再同步外部副本，并用哈希或内容比较验证一致。
- “文档版本”与“软件版本”分开管理，规则见 `docs/WORKFLOW.md`。

## 数据和安全边界

- 只能通过 Eagle Plugin API 或 Web API 修改 Eagle 数据，不直接修改 `.library` 内部的 `metadata.json`。
- Eagle 负责素材保存；SQLite 负责项目、包装、版本、预览和源文件的稳定映射。
- 不依赖同名文件建立唯一关系。
- 不提交 `.library`、`.aep`、ZIP 源文件、SQLite 运行库、日志、密钥或 API Token。
- 图片发送外部 AI 服务前必须取得用户明确许可。
- 机器人发送源文件前必须经过原查询人确认，并保持幂等。

## 当前产品边界

- 第一阶段优先完成项目名、别名、类型和标签驱动的文字检索闭环。
- 图片识别是后续辅助召回能力，不能绕过人工确认。
- AE 包装提取辅助工具属于第五阶段，只能在副本或收集后的工程上处理用户明确选择的合成，并输出 PNG、ZIP 和 manifest；不得直接精简或覆盖原工程。
- Eagle 入库助手插件属于第五阶段；本地一个项目包装文件夹对应 Eagle `00_待入库` 下的一个批次文件夹，递归扫描只识别允许入库的 PNG、ZIP 和 manifest，不原样镜像项目目录；插件负责入库，X.bot 服务负责飞书消息和文件发送。
