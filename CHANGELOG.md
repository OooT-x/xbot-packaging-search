# 变更日志

本文件记录可交付变化。软件版本与核心文档版本独立管理；在首个可运行闭环完成前，变更记录保留在 `Unreleased`。

## Unreleased

### Fixed

- 语境延续（如“看看背景”）不再取消前一条查询：多条候选查询可并存，回复任一旧预览仍能确认（未发送的候选正常发送，已发送的提示已发过），只有 20 分钟过期或用户明确拒绝才失效。
- 修复多候选查询中确认一个选项后，其他选项被误判为“同一份源文件已发送”的问题：源文件幂等改为按单个候选记录，同一查询内每个候选可分别发送，重复确认同一候选才拦截；已完成的历史查询不再接受任何候选发送，并补充确认流程错误日志。
- 预览/提示消息下的回复支持语境延续：非确认词但带包装类型（如“看看背景”）会继承当前查询的项目，切换到新类型候选并重新发送预览；无回复引用时同样回退到最近待确认查询。
- 修复预览图下回复确认但飞书不携带回复引用时无法继承查询语境的问题：确认词没有回复引用时回退匹配该群最近一条待确认查询（仍校验原查询人和 20 分钟有效期）。
- 修复飞书回复幂等键超过 50 字符上限导致包装查询/源文件发送失败的问题：`idempotencyKey` 与 `safeIdempotencyKey` 统一改为 48 字符确定性哈希，保持同事件幂等语义。

### Changed

- 核心文档升级为 v1.7：补充多终端共享 NAS Eagle Library 的目标部署方案，明确集成插件直接通过 Eagle 官方 Plugin API 写入共享库，中央索引服务单点写入数据库；本地 outbox 仅用于事件重试。
- 明确多终端模式不允许各客户端直接打开 NAS 上的 SQLite；现有本地 SQLite 和指定库目录只读同步保留为单机兼容模式，并新增共享库并发、断线恢复、重复事件和索引对账验收要求。
- 核心文档升级为 v1.6：补充大模型语义判断兜底、多候选合并预览、语境延续与多查询并存、候选级幂等、磁盘库只读同步、超限源文件云盘链接降级，并同步触发、确认、状态管理和验收标准。
- 超限源文件自动降级为飞书云盘链接：确认后 bot 把大于约 25MB 的 ZIP 上传到自身云盘并回复下载链接，小文件仍直接发送。
- 包装索引同步改为默认只读解析固定 Eagle 库目录（`E:\Eagle资源库\包装.library`，可用 `LARK_BOT_EAGLE_LIBRARY_PATH` 改写），不再依赖 Eagle 当前活动库；Eagle 界面可停留在其他库，库内数据不被修改。
- 候选回复文案优化：候选行只显示项目、包装名和版本（如“变速箱 · 小标注（v01）”），不再显示包装类型。
- AI 提供方默认固定为 DeepSeek：`start-feishu-bot.ps1`、`scripts/lark-bot-listener.ps1` 默认参数改为 `deepseek`，代码内 `inferAiProvider` 不再自动回退 Ollama/OpenAI（无显式配置时统一 DeepSeek）。
- 多候选预览合并为一条飞书富文本消息：编号文本与图片同条展示，确认改用数字（如“第二个”）选择，“这个”在多候选时提示明确选择；单候选保持单图回复。
- 放宽包装查询触发：动作词与领域词命中其一即可，项目名命中作为主闸门；新增“要/需要/发一下/有没有”等动作词和“素材/模板/框/标注条”等领域词，同时保留 `@X.bot` 门控与普通闲聊不误触发。
- 包装候选回复去掉“依赖警告”标签，并将提示文案调整为更自然的口语表达（单候选与多候选提示分开）。
- 核心文档升级为 v1.5，允许正式入库后清理递归确认全空的批次文件夹；当前官方 API 无删除能力时保留目录并由用户在 Eagle 界面手动删除。
- 核心文档升级为 v1.4，明确已打包 ZIP 的依赖必要性由上游 AE 提取与打包环节负责；Eagle 入库只检查文件可读取、格式和唯一配对。
- 固化正式素材命名、目录、标签、`package_id`、`batch_id`、配对 Eagle ID 和描述字段规则。
- 清理首批 6 个正式素材描述中的审核确认和 ZIP 校验流程话术，只保留稳定元数据与字体、第三方效果等依赖事实。

### Added

- 包装查询接入大模型语义判断兜底：规则关键词未命中但消息 `@X.bot` 时，调用当前 AI 提供方（DeepSeek/Ollama/OpenAI）判断是否为包装查询并提取项目与类型，再走候选流程；AI 不可用或判断失败时自动回退原规则，可用 `LARK_BOT_PACKAGING_AI_JUDGE=off` 关闭。
- 迁入既有 X.bot Node 监听器，并新增包装素材专用 Eagle 同步、SQLite 稳定映射、项目/类型/标签检索、PNG 候选、原查询人确认、20 分钟过期、ZIP 原消息回复和幂等发送模块。
- 新增首批真实“变速箱”素材同步命令、项目别名配置、SQLite 迁移、PowerShell 启动/自动启动入口，以及 19 项自动化测试。
- 建立长期项目管理规则 `AGENTS.md`。
- 建立项目状态页、路线图、决策记录和开发工作流。
- 明确 Git 主版本文档与外部工作副本的同步规则。
- 将 AE 工程中的包装合成选择、预览生成、依赖收集、风险检查和 manifest 输出纳入第五阶段工具链。
- 明确 AE 提取只处理工程副本，并与 Eagle 入库助手形成上游提取、下游入库的职责边界。
- 增加文件夹级批次导入规则：一个本地项目包装文件夹对应一个 Eagle 待入库批次，递归扫描但不原样镜像全部内容。
- 增加入库预检四状态和 `batch_id` 重复导入保护要求。

### Operational data

- 通过 Eagle Web API V2 完成首个真实批次“变速箱包装”入库：将“小标注”“视频框”“背景”3 组 PNG + ZIP 统一登记为 `v01`，移动到对应预览图与 AE 源文件正式目录。
- 为 6 个素材写入项目、包装类型、AE 合成、`package_id`、`batch_id`、配对 Eagle ID、版本、字体及第三方效果警告；按用户确认，本环节不判断 ZIP 内依赖素材是否必要。
- 在原正式 Eagle ID 已不存在的状态下，按 v1.4 规则重新入库“变速箱包装”：待入库素材清零，正式目录恢复为 3 组 PNG + ZIP，且描述中不包含审核确认或 ZIP 校验流程话术。
- 确认错误递归导入项目已退出活动库后，使用干净批次 `batch-MSZQTI9M9MJOZ` 按 v1.5 规则重新入库“变速箱包装”：复用稳定 `package_id`，更新当前 PNG/ZIP 配对 Eagle ID，活动库仅保留 6 个正式素材，待入库素材为 0。
- 已递归确认“变速箱包装”及其 `Root_Collected_Projects` 子目录为空；因当前 Eagle 官方 API 不提供文件夹删除方法，目录保留并等待用户在 Eagle 界面手动清理。

### Project baseline

- 初始化本地 Git 仓库。
- 纳入 v1.1 核心项目文档。
- 建立 README、忽略规则和文本属性配置。
- 完成 Eagle 基础目录、标签组和项目优先的待入库结构。

### Milestones

- M1 文字检索闭环完成：测试群端到端实测通过（查询、候选、确认、直发与云盘链接），进入 M2 版本与模糊输入。

### Project baseline

- 配置 GitHub 私有远程仓库 `OooT-x/xbot-packaging-search`，推送 `codex/feishu-bot-m1` 并建立跟踪。
