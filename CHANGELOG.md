# 变更日志

本文件记录可交付变化。软件版本与核心文档版本独立管理；在首个可运行闭环完成前，变更记录保留在 `Unreleased`。

## Unreleased

### Changed
- 仅图片背景登记与查询分流（2026-09-04）：扫描器允许只有 PNG 且类型明确为“背景”的记录进入正式 `01_预览图/背景`；SQLite 源文件字段可为空，Eagle 注释写明“源文件：无（仅图片背景）”。X.bot 只发送预览并明确没有工程 ZIP，不创建确认查询或进入源文件发送流程；信息条、视频框和分镜排版仍阻止缺 ZIP。新增扫描、迁移、正式入库和服务回归测试。
- AEP 同名合成预览隔离（2026-09-04）：预览链路传递稳定合成 ID；发现工程内同名合成时，在临时 AEP 副本中只重命名目标合成，再用唯一名称调用 `aerender`，原始工程保持不变并在日志记录隔离动作。新增重复名称定位回归测试；真实埃安工程已验证临时副本按 ID 改名，当前机器的 `aerender` 现场渲染仍受超时限制。
- 已入库包装管理与素材修订（2026-09-04）：Eagle 插件新增“已入库管理”工作区，可按项目/关键词查看正式 PNG + ZIP；更新预览图或 ZIP 时按“先写新、再切换、后归档旧”执行，旧 item 保留到 `03_历史版本/<类型>`，事件队列新增 `revision_id` 等字段，SQLite 新增 `package_revisions` 审计表；新增分Part板、报道、时间轴类型和查询别名；“发布新版本”会生成递增版本与 `base_package_id` 草稿。插件版本更新为 `1.6.0`。
- Eagle 插件 AEP 共享引用选择与收集去重（2026-09-04）：结构树中的每个 `ROOT` / `PRE` / `LINK` 层级实例均可独立勾选；收集前对同一合成被多个层级选择的情况弹出明确提示，确认后插件、Worker 和收集核心按合成 ID 自动去重，保证每个合成只生成一份独立收集包；同时修复折叠分支提前占用共享引用首次位置的问题；插件版本更新为 `1.5.11`。
- Eagle 插件 AEP 收集真实进度（2026-09-04）：Worker 通过独立进度事件上报每个合成的收集、预览和完成阶段；插件底部显示真实完成数/百分比、当前合成和阶段，右侧 Collection queue 同步更新每项状态；最终 JSON 结果解析保持兼容；插件版本更新为 `1.5.10`。
- Eagle 插件 AEP 收集可随时停止（2026-09-04）：底部操作栏新增“停止收集”，通过 AbortSignal 终止当前 Worker，并在 Windows 下按进程树结束 Worker 及其渲染子进程；取消后不进入配对预检，界面恢复为可重新选择状态；插件版本更新为 `1.5.9`。
- Eagle 插件 AEP 结构树取消当前选择（2026-09-04）：在“全选当前”旁增加“取消选择”，按当前搜索/筛选结果批量移除已选合成，不影响其他筛选条件下的队列；底部“清空选择”继续用于全部取消；插件版本更新为 `1.5.8`。
- Eagle 插件入库工作台预览交互（2026-09-04）：PNG 原图弹窗复用 AEP 预览控制器，支持以指针为中心的滚轮缩放、左键拖拽平移、双击切换适应/放大以及适应、放大、缩小控制；ZIP 信息弹窗保持原有行为；插件版本更新为 `1.5.7`。
- Eagle 插件 AEP 预览大图交互（2026-09-04）：代表帧支持独立大图预览；滚轮以指针位置为中心连续缩放，缩放后可按住左键拖拽平移，双击切换适应/放大，预览画布拦截页面滚动并提供适应、放大、缩小控制；插件版本更新为 `1.5.6`。
- Eagle 插件底部操作栏窗口级固定（2026-09-04）：移除工作区切换动画中的 `transform` 定位上下文，确保 AEP 收集栏和包装导入栏真正相对插件窗口底部固定，页面滚动不会改变其位置；插件版本更新为 `1.5.5`。
- Eagle 插件底部操作栏悬浮（2026-09-04）：AEP 收集栏和包装导入栏固定在插件窗口下侧，页面滚动时持续可见；对应内容区增加底部安全空间，窄屏自动收紧边距；插件版本更新为 `1.5.4`。
- Eagle 插件配对卡片类型配色与排序修复（2026-09-04）：卡片颜色改为以显式包装类型为准，避免名称中的“视频”等关键词覆盖用户选择；配对列表按信息条、视频框、背景、分镜排版稳定排序，修改类型后立即移动到对应位置；插件版本更新为 `1.5.3`。
- Eagle 插件配对卡片类型补充（2026-09-04）：PNG + ZIP 配对卡片新增卡片内包装类型下拉框，支持四类包装和“待补充类型”；缺失或无效类型会阻止直接入库，选择有效类型后即时更新 `01_预览图/<类型>`、`02_AE源文件/<类型>` 目标和卡片状态；插件版本更新为 `1.5.2`。
- 未知项目包装查询引导（2026-09-04）：当用户明确调取包装但项目名或别名未命中 Eagle 索引时，机器人用更自然的口吻列出当前可查的正式项目名并给出查询示例；用户回复提示追问“有哪些项目”时直接返回同一目录，不再落入普通闲聊；不创建候选确认记录、不发送预览或源文件；项目列表来自刷新后的活动包装索引，空索引时返回明确提示。
- AE Collect Files Report 正文解析（2026-09-04）：新增 `eagle-plugin/lib/report.js`，兼容 UTF-8、UTF-16LE/BE 和 GB18030 文本，识别项目、合成、收集文件、缺失素材、字体、效果/插件和备注章节。扫描器按 manifest 的 `report_files`、同目录、相关收集子目录或最近父目录关联 Report，保留解析状态、编码和路径；字体/效果显示依赖警告，缺失素材阻止入库，Report 与依赖文件仍保持“仅校验”。新增 5 项 Report 解析/扫描回归测试。
- Eagle 插件 AEP 合成层级可读性优化（2026-09-04）：结构树为根合成、直属预合成和共享引用增加 `ROOT` / `PRE` / `LINK` 标识；按真实层级动态缩进并绘制连接线，根合成增加分组视觉，工具栏补充“勾选仅生成独立交付包”的语义说明；收集逻辑、合成 ID 选择状态和窄屏布局保持不变。
- Eagle 插件 AEP 工作台布局收敛（2026-09-03）：AEP 收集标签调整到入库工作台之前；删除结构筛选、检查器和树工具栏中的重复辅助小字，将筛选数量改为悬停提示；“已选合成”底栏改为始终悬浮在页面底部，并为主内容预留滚动空间。
- Eagle 插件配对与 AEP 页面统一排布（2026-09-03）：将 PNG/ZIP 配对卡片调整为左侧约 65%、右侧约 35% 的视觉比例；AEP 收集页改用与入库工作台一致的工程选择卡、输出目录、指标摘要、结构主卡片和底部操作栏，并增加合成总数、推荐交付、已选收集和复核数量摘要。
- Eagle 插件主工作区标签与拖拽扫描优化（2026-09-03）：将“AEP 收集”和“入库工作台”提升为顶部并列主标签，切换时同步工作区状态；导入区补上 `dragenter` / `dragover` / `drop` 事件，支持读取 Eagle 返回的文件夹路径、拖入文件的共同目录和 URI/文本路径兜底，拖拽时显示状态，拖入有效文件夹后自动进入扫描；插件补丁版本更新为 `1.5.1`。
- Eagle 插件内置 AEP 收集工作台（2026-09-03）：新增本机 `XbotAepWorker.exe` 无界面 Worker，插件内可直接选择 AEP、读取真实合成 parent/child 结构、筛选和勾选独立交付对象，支持单合成预览；收集后自动生成 PNG、ZIP、manifest 并回到现有配对预检，再通过 Eagle 官方 Plugin API 直接入库。独立 Tkinter 收集器保留为兼容入口，插件版本更新为 `1.5.0`。
- Eagle 插件配对卡片可读性优化（2026-09-03）：在当前生产插件 `1.5.0` 上放大 PNG/ZIP 预览区域，右侧补充正式入库位置并调整为完整信息布局；整体提高界面字号、行高和操作按钮尺寸，桌面端左右排布、窄屏上下堆叠，并引入参考图的白底、深色描边和柔和彩色卡片语言。
- Eagle 插件界面信息收敛与深色模式修复（2026-09-03）：移除顶部重复说明和导入页长说明，工作流及配对卡片的辅助信息改为悬停提示，简化卡片底部文案；补齐参考图风格下的深色主题映射，避免白底、黑字和浅色卡片覆盖深色模式。
- Eagle 插件导入即正式入库（2026-09-03）：生产插件升级到 `1.4.5`，将选择文件夹、PNG/ZIP 配对、项目/包装名称确认和 Eagle 写入合并为一条流程；确认后直接写入 `01_预览图/<类型>` 与 `02_AE源文件/<类型>`，不创建新的 `00_待入库` 中间批次。重复导入仍保留复用、更新、新建三选项，旧待入库批次通过“历史待入库”入口兼容处理。
- Eagle 配对替换实时预览（2026-09-03）：生产插件升级到 `1.4.4`，修正应用按钮被卡片事件拦截的问题；选择新的 PNG/ZIP 后立即更新卡片文件名、预览和“待应用”状态，点击应用后才正式写入配对。
- Eagle 配对候选下拉修复（2026-09-03）：生产插件升级到 `1.4.3`，修正扫描文件的 `png/zip` 类型与界面 `preview/source` 角色之间的筛选映射；现在“更换 PNG”和“更换 ZIP”菜单会正常显示当前扫描目录中的候选文件。
- Eagle 项目标签组自动同步（2026-09-03）：生产插件升级到 `1.4.2`，导入待入库批次和正式入库前都会通过 Eagle 官方标签组 API 确保“项目”分组存在，并把当前项目标签增量加入其中；不覆盖既有项目标签，也不直接修改 `.library` 文件。
- Eagle 插件资产查看与分侧配对（2026-09-03）：生产插件升级到 `1.4.1`，点击 PNG 进入原图预览，点击 ZIP 查看文件与配对事实；将“更换配对”拆到预览图和打包文件卡片上，分别更换 PNG 或 ZIP，另一侧保持不变，应用后继续执行唯一性和冲突校验。
- Eagle 插件导入配对 UI（2026-09-03）：生产插件升级到 `1.4.0`，将导入预检改为预览图与 ZIP 成对卡片，显示真实文件名和匹配来源；支持在卡片内直接修改包装名称、从受控候选中更换 PNG/ZIP、恢复自动匹配，并在用户覆盖后重新计算缺失和重复引用冲突。新增页面流程条，移除重复的顶部工作区标签导航，正式入库、批次记录和诊断仍保留可达入口。
- Eagle 导入配对预览交互收敛（2026-09-03）：移除顶部工作台标签页，保留页面内四步流程条；包装名称改为卡片内直接可编辑，名称修改即时同步到右侧检查器，配对和写入待入库动作保持不变。
- Eagle 导入配对预览 UI（2026-09-03）：新增 `ui-preview/xbot-eagle-import-pairing.html`，用预览优先的成对卡片展示 PNG 与 ZIP 的自动匹配结果；加入自动匹配、人工覆盖、待确认一对多候选和缺少文件四种状态，支持逐条更换 PNG/ZIP、恢复自动匹配、筛选、搜索、状态汇总、主题切换和写入待入库前的阻止校验。该文件只用于交互方向验证，不替换生产插件。
- AEP 收集器 UI 视觉对齐（2026-09-03）：生产 Tkinter 界面按 `ui-preview/xbot-aep-composition-explorer.html` 重排顶部栏、工作区标签、筛选侧栏、结构/工程目录切换、树表、检查器和底部提取栏；加入真实 X.bot 标识、AEP 文件标识、数量徽标、键盘操作提示、预览占位细节和更接近预览稿的字号/间距。保留现有解析、预览、预合成导出、收集队列和原工程只读边界。
- AEP 收集器启动修复（2026-09-03）：修正收集队列页 `tk.Frame` 的 `pady` 参数，将仅适用于 `pack/grid` 的二元间距移到 `grid` 布局调用，避免新版 GUI 启动时报 `_tkinter.TclError: bad screen distance "0 14"`。
- AEP 收集器合成结构浏览器（2026-09-03）：将 `aep-collector/xbot_aep_collector/gui.py` 从平面合成表格更新为三栏工作台。主视图按真实父级/子级关系提供可展开结构树，保留全部合成平铺列表备用；新增可能交付、顶层、直属预合成和多处复用筛选，复选框按合成 ID 维护独立交付选择，支持 Space、Ctrl+A、Enter 与方向键快捷操作；右侧检查器展示预览、父级使用和直属预合成，底部提取队列显示逐项状态。现有预览、收集、PNG/ZIP/manifest 和原工程只读边界保持不变。
- AEP 合成结构浏览器 UI 预览（2026-09-03）：新增 `ui-preview/xbot-aep-composition-explorer.html`，将复杂 AEP 的合成关系以可展开结构树展示，并加入工程目录视图、名称/类型/尺寸搜索、推荐交付筛选、复选框多选、共享合成标记、右侧详情检查器和提取队列；原型保留合成 ID 级别的全局选择语义，明确区分收集对象与预览来源，并提供主题、密度和结构专注模式 Tweaks。该文件只用于交互方向验证，不替换现有 AEP 收集器生产 GUI。
- Eagle 插件 UI 修复（2026-09-03）：为导入预检卡片增加固定 72×76 缩略图容器并限制图片填充，修复纵向预览图撑高整张卡片的问题；修复导入视图多余 `</div>` 导致正式入库 section 脱离 `.app`、内容整体下移的问题；正式入库视图改用原生 `hidden` 管理显示状态，并在切换时回到页面顶部；详情检查器使用 contain 保留预览比例，插件补丁版本更新为 1.3.1，并新增 HTML 结构回归测试。
- Eagle 入库插件工作台（2026-09-03）：将 `eagle-plugin/index.html` / `js/plugin.js` 更新为批次审核工作台，拆分导入、正式入库、批次记录和系统诊断视图；正式入库新增规则命名、逐条自定义、非法字符/结尾空格/重复名称校验，以及缺失包装类型的检查器补全。`fileBatch` 在任何移动操作前校验并写入自定义 PNG/ZIP 名称，稳定 `package_id`、`batch_id` 和配对关系保持不变；窗口尺寸和 manifest 版本同步更新为 1.3.0。
- UI 交互预览原型（2026-09-03）：新增 `ui-preview/xbot-packaging-workbench.html`，以统一工作台预览 Eagle 批次审核、AEP 合成浏览、代表帧调整和非阻塞收集队列；原型包含筛选、右侧检查器、重复批次三选一、状态反馈、历史与诊断页面、主题/密度 Tweaks，以及“批量规范命名”环节（规则生成、逐条自定义、非法字符/重复名称校验、预览图与源文件配对、manifest 同步提示）。该文件只用于 UI 方向验证，不替换现有 Eagle 插件和 AEP 生产界面。
- 项目整理（2026-09-03）：以已完成 AEP 收集器、Eagle 插件、事件同步和 CI 的主线统一项目状态；核心文档升级为 v1.27，补充多终端共享 NAS 的插件直写 Eagle、中央索引服务单点写入、outbox 重试和现场验收边界，并建立 D-041。
- 状态与文档同步（2026-09-03）：核心文档入口统一为 v1.26；按当前运行索引更新为 4 个项目、19 组包装、配对错误 0，并将 M5 剩余项收敛为 Collect Files Report 正文解析与真实 Eagle→bot 现场验证。
- Eagle 标签整理（2026-09-02）：通过 Eagle 官方标签组接口新增黄色“项目”分组，并将“支付宝阿宝”“华为途灵”“乾崑奕境”“变速箱”4 个项目名标签归入该组；素材上的原标签关联保持不变。核心文档升级为 v1.24。
- 插件入库结果同步（2026-09-02）：正式归位后由插件把批次元数据、`package_id`、PNG/ZIP Eagle ID 和路径追加到共享 `packaging-ingest-events.jsonl`；bot 监听器作为 SQLite 唯一写入方消费事件，按 `event_id` 去重，写入 `batches`/`batch_details` 后强制刷新 Eagle 索引，失败事件保留并自动重试。新增 1 个桥接测试、1 个小型重试集成测试和 1 个批次事件状态测试；当前 96 项 JS + 18 项 Python 测试通过。真实 Eagle UI → 常驻 bot 的现场联调仍待执行。
- 核心文档升级为 v1.23：新增 D-037，明确插件通过共享 JSONL 事件队列把正式入库结果交给 bot 单写入 SQLite，并以 `event_id` 保证幂等和失败重试。
- 项目约束：后续测试采用精简、精准策略，只覆盖新增行为、关键边界和回归风险；不重复铺设低价值用例，也不以测试数量作为完成指标。
- 真实 AE 主程序验收（2026-09-02）：在 AE 2025 中打开真实“阿宝包装.aep”收集工程，覆盖“背景”“包装”“人名条”“视频框”“竖屏框”。表达式未见报错，收集工程内素材链接正常，最终画面与预览来源一致；字体和第三方效果未出现阻止性缺失，已有依赖警告继续写入 manifest/描述并要求接收环境人工确认。
- 重复导入保护（2026-09-02）：新增 `batches`/`batch_details` SQLite 迁移和批次身份记录；插件按 `batch_id`、稳定 `batch_key`、源路径或 manifest 标识检测重复，默认先停下并提供“复用现有批次、更新现有批次、新建独立批次”三种选择。复用不新增素材，更新刷新现有批次元数据，新建使用独立批次 ID 和文件夹；新增 6 项导入模式回归测试和 3 项数据库测试。
- 飞书消息兼容：带文字的 `post` 富文本规范化后进入现有 AI 对话、包装查询和候选确认链路；纯图片在私聊、有效提及或群聊回复 X.bot 时返回诚实的未识图提示。群聊低噪音门控保持不变，图片二进制不下载、不发送外部 AI。新增回归测试后 85 项 JS + 18 项 Python 测试全部通过。
- 文档归档：将暂不实施的 Codex 路线升级方案沉淀到 `docs/deferred/飞书机器人-Codex路线升级方案.md`，不纳入 `docs/CONTEXT.md`，现有 DeepSeek 和飞书包装流程保持不变。
- 飞书权限：发布 X.bot 的 `im:message.group_msg` 后，bot 身份群历史列表接口恢复；重启 `FeishuBot` 后事件总线保持 `running`、`active_consumers=1`。
- 运行保障：监听器遇到 `event consume` 非零退出时记录完整错误并按 5 秒起步、最多 60 秒退避重试，不再因单次事件消费者异常直接退出；重新注册 `FeishuBot` 登录自启任务，并通过无编码依赖的目录发现逻辑显式复用既有飞书运行根目录。
- 运行态验收：2026-08-24 受控终止事件消费者后，父进程自动拉起下一轮监听，`lark-cli event status --json` 保持 `running` 且 `active_consumers=1`。
- 活人感增强：系统提示从 20+ 条规则精简为 12 条核心原则，聚焦"变化"和"接话"；新增回复风格变化追踪（`analyzeRecentStyle`），检测连续确认式开头/语气词开头/回复长度单一等模式并注入变化提醒；包装查询主动建议和意图分类器保持不变。：(1) 情绪感知扩展——新增烦躁/疲惫模式匹配，系统提示自动注入情绪上下文，引导 AI 根据用户情绪调整回复风格和长度；(2) 结构化意图分类——新增轻量级消息意图分类器（query/emotion/work/chat），与主回复并行执行并记录日志，为后续路由决策提供数据；(3) 包装查询主动建议——查询成功后自动生成相关类型的后续建议，引导用户发现更多素材。
- 整理项目本地产物：删除 `aep-collector/` 下可重建的 `.build-venv`（约 62MB 构建虚拟环境）、`build`（PyInstaller 中间产物）与 `__pycache__` 缓存，以及 `dist/AE-Preview-Bridge` 源码冗余副本（原版见 `ae-bridge/XbotPreviewBridge.jsx` 与 `install-preview-bridge.ps1`）；保留当前可用交付物 `dist/XbotAepCollector.exe`、活跃数据库 `data/packaging.sqlite*` 与 `runtime/logs`。`aep-collector` 由约 130MB 精简至约 28MB，git 工作区保持干净，重建命令见 `aep-collector/README.md` 与 `build.ps1`。
- 新增 `docs/CONTEXT.md` 轻量上下文速览（状态、里程碑、模块地图、决策指针、数据/安全边界、常用命令），并接入 `AGENTS.md` 必读顺序与 `README.md` 项目管理入口，后续迭代先读速览、命中对应主题时才翻全文，降低每次恢复上下文的阅读量。
- 在 `startQueryFromResult` 增加逐步诊断日志（`packaging dbg:` 覆盖图片上传/构建 post/发送 replyPost 各节点），用于定位突发崩溃；并在带与不带 `-Voice` 两种模式下实测完整包装查询闭环（候选富文本 → 用户确认 → ZIP 直发/云盘降级），两轮均稳定通过、旧崩溃未复现，`replyPost`、ZIP 发送与语音 TTS 环节均排除为崩溃元凶。


- ?? bot ???????????????? `FeishuBot` ??????????? `scripts/start-feishu-bot-autostart.ps1`??`Get-ExistingBotProcess` ? pid ?????????? `lark-bot-listener.js` ????????????????????????? bot?
- 核心文档升级为 v1.21：删除 7.1 节残留的“无回复引用回退最近待确认查询”旧规则，使触发优先级与 D-032、消息流程、异常处理和当前实现一致；状态页同时区分首批 1 项目 / 3 包装记录与当前 2 项目 / 9 包装索引。
- 单候选和多候选统一为一条飞书富文本消息，候选名称、对应 PNG、回复选择方式和20分钟有效期不再拆分发送。
- 核心文档升级为 v1.20，新增 D-033，将候选富文本消息定义为唯一的确认和语境延续入口。
- 包装候选确认、拒绝、数字选择和类型追问现在必须回复机器人发出的候选或提示消息；删除无引用消息回退最近 `pending` 查询的路径，避免历史查询污染新请求。
- 核心文档升级为 v1.19，新增 D-032，正式取代 D-018、D-024 中的无引用回退规则。

### Added

- GitHub Actions 持续集成（2026-09-03）：新增 `.github/workflows/ci.yml`，在 push、pull request 和手动触发时使用 `windows-latest`、Node.js 24 与 Python 3.14，自动安装 `aep-collector/requirements.txt` 并运行 `npm test`、`npm run check`。

- 视频框父级预览取帧修复（2026-09-02）：默认不再盲目渲染父级合成的绝对 2 秒，而是读取视频框图层的 `in_point`，取其出现后的第 2 秒；短于 2 秒时回退到图层仍可见的最后一帧。预览 manifest 新增父级图层时间与实际取样偏移记录，新增 3 项 Python 回归测试。

- 外部 AEP 收集器新增直属预合成功能（2026-09-02）：选中一个合成后可在 GUI 中一键查看直接作为图层源的预合成，也可将这些预合成分别收集为独立 AEP、素材目录、代表帧 PNG、manifest 和 ZIP；新增 `list-precomps`/`precomps` 与 `collect-precomps` CLI 入口。导出保持原有副本安全边界，每个预合成继续递归保留自己的下层依赖，不把父级包装合成混入子包；新增 2 项 Python 行为测试。

- `aerender` 高质量预览优先调用 `Xbot PNG with Alpha` 输出模块直接生成 PNG，校验 PNG 签名和 IEND 尾标后再原子替换目标文件；模板缺失或失败时自动回退 TIFF+Alpha 中转。
- AE 完全关闭时用真实“阿宝包装.aep”验证新模板：1920×1080 `包装` 直接 PNG 用时 12.36 秒、大小 4,901,244 字节；500×300 `人名条` 用时 10.42 秒，RGBA Alpha 范围 0–255。新增 2 项回归测试，全项目 99 项测试通过。
- 构建新版 `XbotAepCollector.exe`（29,119,138 字节，SHA-256 `52C9C50CF767134EC4CE65B0F795A4350F6D193084B0D16ED1552B48A15AB656`），隐藏启动 6 秒存活检查通过，父子测试进程已全部关闭。
- 新增可选 `XbotPreviewBridge.jsx`：采用常驻 AE + 本地文件 IPC + `saveFrameToPng` 快速刷新交互预览，校验异步写出的 PNG 完整尾标；桥接不可用时自动回退现有 `aerender`。
- 视频框、横屏框和竖屏框默认从唯一合适的上层包装展示合成取景，在背景上展示框体；预览窗口可改选自身或其他直接父合成，manifest 记录收集合成与预览来源的区别。
- 最终入库 PNG 不直接复用快速快照，每个包装只在收集时执行一次高质量 `aerender`，兼顾调帧速度和透明边缘质量；新增桥接模拟、父级推荐和 manifest 回归测试。
- 真实“阿宝包装.aep”测速：1920×1080 常驻桥接连续快照约 2.15–2.25 秒，原高质量 `aerender` 约 13.32 秒；真实“视频框”收集以`包装`为预览来源，生成 5,286,411 字节 ZIP，Eagle 扫描 1 条 ready、0 冲突。全项目 97 项测试通过。
- 构建新版 `XbotAepCollector.exe`（29,119,924 字节，SHA-256 `4F4562C1445AB0C4CA7150E49755DF95579B6D138FC1C2310837A79A3C60B672`）及带 UAC 自提升安装脚本的 `AE-Preview-Bridge` 安装包，EXE 隐藏启动 6 秒存活检查通过。
- 外部 AEP 收集器新增可调代表帧预览窗口：默认第 2 秒，短合成取最后有效帧；支持秒数输入、时间轴拖动和前后逐帧重新渲染，透明画面使用棋盘格预览。
- 新增 `aerender` 后台单帧渲染、AE 自带 TIFF+Alpha 输出、本地 PNG 转换和预乘 Alpha 还原；收集 manifest 记录 `preview_file`、时间和 AE 帧号，ZIP 内 manifest 同步刷新。
- Eagle 扫描器支持收集 manifest 显式预览引用，仍兼容旧版受控名称匹配；子目录依赖 PNG 继续忽略，不误报冲突。
- 真实“阿宝包装.aep”验证了背景 RGB PNG、人名条 RGBA PNG（Alpha 0–255）和完整“收集 → 预览 → manifest/ZIP → Eagle 扫描”临时闭环；全项目 93 项自动化测试全部通过。
- 重新构建预览版 `XbotAepCollector.exe`（29,107,983 字节，SHA-256 `895B842F0DD2E9A4E763E3E663341A1CD66D4CD4849CC1DEEB5FA07EC0AE3A15`），6 秒隐藏启动存活检查通过。
- 新增双 manifest 协议：AEP 收集器输出 `xbot-collection`，AE 正式交付清单输出 `xbot-eagle-ingest`；Eagle 插件兼容没有类型字段的旧版收集记录和正式清单。
- Eagle 入库扫描器可从收集记录读取合成名称、逐合成 ZIP 和依赖状态，并按唯一优先级配对根目录 PNG；新增角括号/尖括号及横竖屏视频框受控别名，歧义继续阻止而不猜测。
- 使用真实“支付宝阿宝”6 张 PNG、6 个 ZIP 和 6 个旧版收集 manifest 验证：形成 6 条可导入记录、12 个将导入文件、6 个仅校验 manifest、0 冲突。新增 1 项回归测试，全项目 87 项自动化测试全部通过。
- 外部 AEP 收集工具现在为每个选中合成自动生成一个展开目录和一个同名 ZIP；ZIP 内以收集目录为顶层，包含精简 AEP、素材和 manifest，并在完成前检查 CRC、AEP 与 manifest 必需项。
- manifest 新增 `collection_directory` 和 `source_file`，收集结果与 GUI 显示 ZIP 路径和大小；压缩失败时清理该次收集目录和半成品 ZIP。新增 2 项 ZIP 回归测试，全项目 86 项自动化测试全部通过。
- 使用真实“阿宝包装.aep / 背景”完成逐合成 ZIP 验证：21,270,338 字节、11 个文件、CRC 正常，AEP 可再次解析，9 个重链接素材路径全部存在。
- 新增 Windows 外部 `aep-collector`：不启动 AE 即打开 AEP，显示全部合成、尺寸、时长、帧率、父级使用关系和内部预合成，并支持多选收集。
- 新增按合成离线收集核心：递归保留嵌套合成和图层素材，在新目录复制并重链接依赖，输出精简 AEP 与 manifest，保存后自动再次解析；不覆盖原工程。
- 基于真实“阿宝包装.aep”验证：识别全部 11 个合成；“背景”收集得到 1 个合成和 9 个有效重链接素材，“包装”收集得到 11 个嵌套合成和 10 个有效重链接素材。
- 新增 `run.ps1`、`build.ps1`、命令行 inspect/collect 入口、第三方许可说明和 5 项 Python 单元测试；已构建并启动验证 `XbotAepCollector.exe`。
- 新增 X.bot AE 包装整理 ScriptUI 面板第一版：读取用户选择的包装合成、逐条编辑包装元数据、递归扫描缺失素材/字体/效果/跨合成表达式风险、生成代表帧 PNG 和标准化 manifest。
- 面板接入 AE 原生 Collect Files 半自动流程，并支持 Windows 收集目录 ZIP 压缩、Report 文件登记和交付完整性检查；默认不执行 Reduce Project，不修改原工程项目项。
- 新增可复用 manifest 核心、安装脚本、AE 运行时探针与 7 项自动化测试；生成 manifest 已通过 Eagle 入库扫描器的无猜测唯一配对测试，全项目测试增至 79 项。
- 面板已安装到本机 AE 2025 并完成源码/安装文件哈希一致性验证；真实 AE `25.6.4x3` 确认 ScriptUI 与 `saveFrameToPng` 可用，面板已从实际安装目录加载，并从当前所选“标注-方括号”合成的 3.8 秒成功输出 18,749 字节 PNG。
- 正式入库支持直接由 Eagle 自带功能导入的项目文件夹：递归读取批次及子目录，对无插件注释的 PNG/ZIP 按受控位置和文件名重新配对，并在缺少类型时提供四类包装选择。
- 正式归位时为手动导入素材补写稳定 `package_id`、`batch_id`、配对 ID、标签和事实描述，并按项目、类型、名称和版本统一整理 Eagle 素材名称。
- 插件拆分为“导入”与“入库”两个独立按钮：导入只落 `00_待入库/<项目>包装` 批次；入库按钮读取指定待入库批次，校验 PNG+ZIP 配对后按类型归位到 `01_预览图/<类型>` 与 `02_AE源文件/<类型>`，替换待入库标签并提示空批次手动清理。
- 新增 6 项正式入库自动化测试，测试总数 55 项全部通过。
- 新建 Eagle 入库助手窗口插件骨架：`eagle-plugin/manifest.json`、logo、文件夹选择、扫描预检、四状态表格和确认导入按钮。
- 实现插件扫描引擎 `eagle-plugin/lib/scan.js`：递归扫描、manifest 优先引用配对、无 manifest 时受控候选配对、`batch_id`/`batchKey`、将导入/仅校验/忽略/冲突四状态。
- 实现 Eagle 导入层 `eagle-plugin/lib/eagle-api.js`：`00_待入库/<项目>包装` 批次文件夹创建与复用、`addFromPath` 导入 PNG/ZIP、D-013 事实字段描述与标签、配对 Eagle ID 回写。
- 新增 10 项插件自动化测试（扫描 9 项、导入 5 项），测试总数 49 项全部通过。

### Changed
- 活人感增强：系统提示从 20+ 条规则精简为 12 条核心原则，聚焦"变化"和"接话"；新增回复风格变化追踪（`analyzeRecentStyle`），检测连续确认式开头/语气词开头/回复长度单一等模式并注入变化提醒；包装查询主动建议和意图分类器保持不变。：(1) 情绪感知扩展——新增烦躁/疲惫模式匹配，系统提示自动注入情绪上下文，引导 AI 根据用户情绪调整回复风格和长度；(2) 结构化意图分类——新增轻量级消息意图分类器（query/emotion/work/chat），与主回复并行执行并记录日志，为后续路由决策提供数据；(3) 包装查询主动建议——查询成功后自动生成相关类型的后续建议，引导用户发现更多素材。

- 核心文档升级为 v1.18：新增 D-031，固化“后台直接 PNG 优先 + TIFF 兼容回退”规则。
- 核心文档升级为 v1.17：新增 D-030，固化“常驻 AE 快速调帧 + 高质量最终渲染”和“视频框使用上层包装展示合成取景”的规则。
- Eagle 入库助手升级到 `1.1.5`；优先按 `xbot-collection.preview_file` 配对收集器自动生成的 PNG，并保持子目录依赖 PNG 为忽略状态。
- 核心文档升级为 v1.16：新增 D-029，固化“默认第 2 秒 + 预览窗口调帧 + 后台 AE 真实渲染 + 显式 PNG/ZIP 配对”。
- Eagle 入库助手升级到 `1.1.4`；只有 `xbot-eagle-ingest` 或兼容的正式清单会触发“存在 manifest 时仅导入明确引用文件”，`xbot-collection` 只作为配对和依赖校验来源。
- 核心文档升级为 v1.15：新增 D-028，明确收集记录与正式入库 manifest 的字段边界、兼容规则和安全配对行为。
- 核心文档升级为 v1.14：逐合成 ZIP 成为外部收集工具的强制产物，新增 D-027，明确 ZIP 结构、manifest 文件名记录、完整性校验、失败清理和 Eagle 候选边界。
- 核心文档升级为 v1.13：AE 包装收集主入口从 ScriptUI + 原生 Collect Files 改为外部 AEP 合成浏览与收集工具；D-025 标记已取代，新增试行决策 D-026。BHY 仅作行为参考，不打包其闭源 EXE。
- 核心文档升级为 v1.12：确定 AE 包装整理采用“ScriptUI 面板 + AE 原生 Collect Files + 默认不 Reduce Project”的安全边界，并新增决策 D-025。
- 核心文档升级为 v1.11：固化“明确的新包装查询优先于历史候选确认”的消息路由规则，补充无回复引用回退边界、过期候选处理、异常表和验收用例，并新增决策 D-024。
- 核心文档升级为 v1.10：插件在文件名、目录名、标签和 manifest 类型中识别“标注”“人名条”时直接规范化为“信息条”；新增决策 D-023，插件版本升至 `1.1.3`。
- 核心文档升级为 v1.9：明确“导入”是可选搬运助手，“入库”以 Eagle 待入库目录为事实来源并可独立工作；新增决策 D-022。
- 核心文档升级为 v1.8：明确 Eagle 入库助手采用“导入待入库 → 正式入库”两段式操作，正式归位由独立“入库”按钮执行；新增决策 D-021。
- 核心文档升级为 v1.7：将 Eagle 入库助手插件从“已规划”调整为“实施中”，明确插件目录结构与首批实现范围（扫描→预检→导入待入库批次），正式归位与索引同步作为下一增量。

### Fixed

- 修复 `py-aep 0.10.2` 在 Reduce Project 删除当前活动合成后仍把旧 ID 写入 `fcid`、导致收集 AEP 无法再次解析的问题；收集器改为把活动项目与二进制引用同步指向保留的目标合成。
- 修复临时目录改名后 AEP 内素材绝对路径仍指向旧临时目录的问题；收集过程改为在唯一的新目录内完成并以未完成标记保护，成功后所有重链接路径保持有效。
- 修复 AE `saveFrameToPng` 已异步写出 PNG、但 ExtendScript `File.exists` 尚未刷新时被误报为预览失败的问题；改为重新实例化并短暂轮询文件状态，已用真实合成复现并验证。
- 修复明确的新包装查询（如 `@X.bot 给我变速箱的背景`）被确认流程绑定到历史待确认记录、错误回复“候选已经超过 20 分钟”的问题；显式 `@mention + 已知项目` 现在跳过确认回退并正常新建查询，纯确认和无项目类型追问仍保留原语境能力。
- 修复 `v1.1.1` 扁平化 Eagle Folder 实例时，使用对象展开丢失 getter 提供的 `name`、`description` 和 `children`，造成插件误报“Eagle 中还没有 00_待入库 目录”的问题；插件版本升至 `1.1.2`，并增加真实 Folder 实例形态的回归测试。
- 修复真实 Eagle Folder Plugin API 返回树形文件夹时，插件只遍历顶层目录、导致 `00_待入库` 的手动导入批次不进入扫描范围的问题；适配层现在会递归扁平化文件夹树、补全父子关系，并兼容素材 `folders` 字段返回文件夹对象。插件版本升至 `1.1.1`，全项目 68 项测试通过。
- 修复“刷新批次”在同一待入库文件夹混有多个 `batch_id` 时把相同 `package_id` 合并判重、最终显示 0 对可入库的问题：PNG/ZIP 现在按 `batch_id + package_id` 配对，不完整旧批次不再遮蔽完整新批次；若多个批次都完整则继续阻止，避免重复入库。
- 批次读取改为优先按所选文件夹调用 Eagle `item.get({ folders })`，再查询未分类素材补齐配对；入库预检新增 `batch_id` 列和批次数提示。新增 3 项回归测试，全项目 59 项测试全部通过。
- 修复入库批次读取：改为从全库按文件夹归属召回，并补充“未分类”但配对引用或 `batch_id` 命中批次内素材的文件，避免配对文件未挂到批次文件夹时预检看不到；刷新批次后自动加载选中批次的入库预检。
- 语境延续（如“看看背景”）不再取消前一条查询：多条候选查询可并存，回复任一旧预览仍能确认（未发送的候选正常发送，已发送的提示已发过），只有 20 分钟过期或用户明确拒绝才失效。
- 修复多候选查询中确认一个选项后，其他选项被误判为“同一份源文件已发送”的问题：源文件幂等改为按单个候选记录，同一查询内每个候选可分别发送，重复确认同一候选才拦截；已完成的历史查询不再接受任何候选发送，并补充确认流程错误日志。
- 预览/提示消息下的回复支持语境延续：非确认词但带包装类型（如“看看背景”）会继承当前查询的项目，切换到新类型候选并重新发送预览；无回复引用时同样回退到最近待确认查询。
- 修复预览图下回复确认但飞书不携带回复引用时无法继承查询语境的问题：确认词没有回复引用时回退匹配该群最近一条待确认查询（仍校验原查询人和 20 分钟有效期）。
- 修复飞书回复幂等键超过 50 字符上限导致包装查询/源文件发送失败的问题：`idempotencyKey` 与 `safeIdempotencyKey` 统一改为 48 字符确定性哈希，保持同事件幂等语义。

### Changed
- 活人感增强：系统提示从 20+ 条规则精简为 12 条核心原则，聚焦"变化"和"接话"；新增回复风格变化追踪（`analyzeRecentStyle`），检测连续确认式开头/语气词开头/回复长度单一等模式并注入变化提醒；包装查询主动建议和意图分类器保持不变。：(1) 情绪感知扩展——新增烦躁/疲惫模式匹配，系统提示自动注入情绪上下文，引导 AI 根据用户情绪调整回复风格和长度；(2) 结构化意图分类——新增轻量级消息意图分类器（query/emotion/work/chat），与主回复并行执行并记录日志，为后续路由决策提供数据；(3) 包装查询主动建议——查询成功后自动生成相关类型的后续建议，引导用户发现更多素材。

- 核心文档升级为 v1.6：补充大模型语义判断兜底、多候选合并预览、语境延续与多查询并存、候选级幂等、磁盘库只读同步、超限源文件云盘链接降级，并同步触发、确认、状态管理和验收标准。
- 超限源文件自动降级为飞书云盘链接：确认后 bot 把大于约 25MB 的 ZIP 上传到自身云盘并回复下载链接，小文件仍直接发送。
- 包装索引同步改为默认只读解析固定 Eagle 库目录（`E:\Eagle资源库\包装.library`，可用 `LARK_BOT_EAGLE_LIBRARY_PATH` 改写），不再依赖 Eagle 当前活动库；Eagle 界面可停留在其他库，库内数据不被修改。
- 候选回复文案优化：候选行只显示项目、包装名和版本（如“变速箱 · 小标注（v01）”），不再显示包装类型。
- AI 提供方默认固定为 DeepSeek：`start-feishu-bot.ps1`、`scripts/lark-bot-listener.ps1` 默认参数改为 `deepseek`，代码内 `inferAiProvider` 不再自动回退 Ollama/OpenAI（无显式配置时统一 DeepSeek）。
- 多候选预览合并为一条飞书富文本消息：编号文本与图片同条展示，确认改用数字（如“第二个”）选择，“这个”在多候选时提示明确选择；单候选保持单图回复。
- 放宽包装查询触发：动作词与领域词命中其一即可，项目名命中作为主闸门；新增“要/需要/发一下/有没有”等动作词和“素材/模板/框/标注条”等领域词，同时保留 `@X.bot` 门控与普通闲聊不误触发。
- 包装候选回复去掉“依赖警告”标签，并将提示文案调整为更自然的口语表达（单候选与多候选提示分开）。
- 核心文档升级为 v1.5，允许正式入库后清理递归确认全空的批次文件夹；当前官方 API 无删除能力时保留目录并由用户在 Eagle 界面手动删除。
- 核心文档升级为 v1.4，明确已打包 ZIP 的依赖必要性由上游 AE 提取与打包环节负责；Eagle 入库只检查文件可读取、格式和唯一配对。
- 固化正式素材命名、目录、标签、`package_id`、`batch_id`、配对 Eagle ID 和描述字段规则。
- 清理首批 6 个正式素材描述中的审核确认和 ZIP 校验流程话术，只保留稳定元数据与字体、第三方效果等依赖事实。

### Added

- 包装查询接入大模型语义判断兜底：规则关键词未命中但消息 `@X.bot` 时，调用当前 AI 提供方（DeepSeek/Ollama/OpenAI）判断是否为包装查询并提取项目与类型，再走候选流程；AI 不可用或判断失败时自动回退原规则，可用 `LARK_BOT_PACKAGING_AI_JUDGE=off` 关闭。
- 迁入既有 X.bot Node 监听器，并新增包装素材专用 Eagle 同步、SQLite 稳定映射、项目/类型/标签检索、PNG 候选、原查询人确认、20 分钟过期、ZIP 原消息回复和幂等发送模块。
- 新增首批真实“变速箱”素材同步命令、项目别名配置、SQLite 迁移、PowerShell 启动/自动启动入口，以及 19 项自动化测试。
- 建立长期项目管理规则 `AGENTS.md`。
- 建立项目状态页、路线图、决策记录和开发工作流。
- 明确 Git 主版本文档与外部工作副本的同步规则。
- 将 AE 工程中的包装合成选择、预览生成、依赖收集、风险检查和 manifest 输出纳入第五阶段工具链。
- 明确 AE 提取只处理工程副本，并与 Eagle 入库助手形成上游提取、下游入库的职责边界。
- 增加文件夹级批次导入规则：一个本地项目包装文件夹对应一个 Eagle 待入库批次，递归扫描但不原样镜像全部内容。
- 增加入库预检四状态和 `batch_id` 重复导入保护要求。

### Operational data

- 通过 Eagle Web API V2 完成首个真实批次“变速箱包装”入库：将“小标注”“视频框”“背景”3 组 PNG + ZIP 统一登记为 `v01`，移动到对应预览图与 AE 源文件正式目录。
- 为 6 个素材写入项目、包装类型、AE 合成、`package_id`、`batch_id`、配对 Eagle ID、版本、字体及第三方效果警告；按用户确认，本环节不判断 ZIP 内依赖素材是否必要。
- 在原正式 Eagle ID 已不存在的状态下，按 v1.4 规则重新入库“变速箱包装”：待入库素材清零，正式目录恢复为 3 组 PNG + ZIP，且描述中不包含审核确认或 ZIP 校验流程话术。
- 确认错误递归导入项目已退出活动库后，使用干净批次 `batch-MSZQTI9M9MJOZ` 按 v1.5 规则重新入库“变速箱包装”：复用稳定 `package_id`，更新当前 PNG/ZIP 配对 Eagle ID，活动库仅保留 6 个正式素材，待入库素材为 0。
- 已递归确认“变速箱包装”及其 `Root_Collected_Projects` 子目录为空；因当前 Eagle 官方 API 不提供文件夹删除方法，目录保留并等待用户在 Eagle 界面手动清理。

### Project baseline

- 初始化本地 Git 仓库。
- 纳入 v1.1 核心项目文档。
- 建立 README、忽略规则和文本属性配置。
- 完成 Eagle 基础目录、标签组和项目优先的待入库结构。

### Milestones

- M1 文字检索闭环完成：测试群端到端实测通过（查询、候选、确认、直发与云盘链接），进入 M2 版本与模糊输入。

### Project baseline

- 配置 GitHub 私有远程仓库 `OooT-x/xbot-packaging-search`，推送 `codex/feishu-bot-m1` 并建立跟踪。
