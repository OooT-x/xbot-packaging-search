{
  "name": "xbot-packaging-search",
  "version": "0.0.0",
  "private": true,
  "description": "X.bot 飞书包装素材文字检索、候选确认与源文件发送闭环",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "sync:eagle": "node bot/sync-eagle.js",
    "check": "node --check bot/lark-bot-listener.js && node --check bot/sync-eagle.js && node --check eagle-plugin/js/plugin.js && node --check eagle-plugin/lib/scan.js && node --check eagle-plugin/lib/eagle-api.js && node --check ae-tool/XbotPackagingOrganizer/manifest-core.js"
  },
  "engines": {
    "node": ">=24"
  }
}
